<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Hello World</title>
    <url>/2022/05/06/hello-world/</url>
    <content><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p>
<span id="more"></span>

<h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tbody><tr><td class="code"><pre><code class="hljs bash">$ hexo new <span class="hljs-string">"My New Post"</span><br></code></pre></td></tr></tbody></table></figure>

<p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p>
<h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tbody><tr><td class="code"><pre><code class="hljs bash">$ hexo server<br></code></pre></td></tr></tbody></table></figure>

<p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p>
<h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tbody><tr><td class="code"><pre><code class="hljs bash">$ hexo generate<br></code></pre></td></tr></tbody></table></figure>

<p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p>
<h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tbody><tr><td class="code"><pre><code class="hljs bash">$ hexo deploy<br></code></pre></td></tr></tbody></table></figure>

<p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>
]]></content>
  </entry>
  <entry>
    <title>Java集合框架</title>
    <url>/2022/12/04/Java%E9%9B%86%E5%90%88%E6%A1%86%E6%9E%B6/</url>
    <content><![CDATA[<p>在实现方法时,选择不同的数据结构会导致其实现风格以及性能存在着很大差异 。 需要快速地搜索成千上万个（ 甚至上百万 ） 有序的数据项吗？需要快速地在有序的序列中间插人元素或删除元素吗？需要建立键与值之间的关联吗?</p>
<span id="more"></span>

<h3 id="集合框架"><a href="#集合框架" class="headerlink" title="集合框架"></a>集合框架</h3><p><strong>Connection接口</strong></p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">Collection</span>&lt;E&gt;<br>{<br>    <span class="hljs-type">boolean</span> <span class="hljs-title function_">add</span> <span class="hljs-params">( E element )</span>;	<span class="hljs-comment">//用于向集合中添加元素</span><br>    Iterator &lt; E &gt; iteratorQ;	<span class="hljs-comment">//返回一个实现了 Iterator 接口的对象</span><br>    ...<br>}<br></code></pre></td></tr></tbody></table></figure>

<p><strong>迭代器</strong></p>
<img src="https://pic.imgdb.cn/item/638b72ac16f2c2beb161603f.png">

<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">Iterator</span>&lt;E&gt;<br>{<br>    E <span class="hljs-title function_">next</span> <span class="hljs-params">()</span>;	<span class="hljs-comment">//逐个访问集合中的每个元素</span><br>    <span class="hljs-type">boolean</span> <span class="hljs-title function_">hasNext</span><span class="hljs-params">()</span>;	<span class="hljs-comment">//判断是否到达末尾</span><br>    <span class="hljs-keyword">void</span> remove;	<span class="hljs-comment">//删除上次调用 next 方法时返回的元素</span><br>    <span class="hljs-keyword">default</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">forEachRemaining</span> <span class="hljs-params">(Consumer&lt;? <span class="hljs-built_in">super</span> E&gt; action)</span>;<br>}<br></code></pre></td></tr></tbody></table></figure>

<p> Java 迭代器是位于两个元素之间 。 当调用 next 时，迭代器就越过下一个元素 ， 并返回刚刚越过的那个元素的引用。</p>
<p>删除字符串集合中的第一个元素</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java">Interator&lt;String&gt; it = c.iterator();<br>it.next()<br>it.remove()<br></code></pre></td></tr></tbody></table></figure>

<p> 对next 方法和 remove 方法的调用具有互相依赖性。如果调用remove 之前，没有调用 next 将是不合法的 。</p>
<p><strong>集合框架中的接口</strong></p>
<p>Java 集合框架为不同类型的集合定义了大量接口</p>
<img src="https://pic.imgdb.cn/item/638b72ac16f2c2beb1616043.png" style="zoom:80%;">

<h3 id="具体的集合"><a href="#具体的集合" class="headerlink" title="具体的集合"></a>具体的集合</h3><img src="https://pic.imgdb.cn/item/638b72ac16f2c2beb1616056.png" style="zoom:80%;">

<h4 id="链表"><a href="#链表" class="headerlink" title="链表"></a><strong>链表</strong></h4><p>链表将每个对象存放在独立的结点中，每个结点还存放着序列下一个结点的引用。</p>
<p>集合类库提供了子接口 Listlterator , 其中包含 add 方法 </p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">interface</span> <span class="hljs-title class_">ListIterator</span>&lt;E&gt; <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Iterator</span>&lt;E&gt;<br>{<br>	<span class="hljs-keyword">void</span> <span class="hljs-title function_">add</span><span class="hljs-params">(E element)</span>;<br>	...<br>}<br></code></pre></td></tr></tbody></table></figure>

<p>Listlterator 接口有两个方法 ， 可以用来反向遍历链表</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java">E <span class="hljs-title function_">previous</span> <span class="hljs-params">()</span><br><span class="hljs-type">boolean</span> hasPreviousO<br></code></pre></td></tr></tbody></table></figure>

<p>LinkedList 类的 listlterator 方法返回一个实现了 Listlterator 接口的迭代器对象</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java">ListIterator&lt;String&gt; iter = staff.listlteratorO <br></code></pre></td></tr></tbody></table></figure>

<p>示例：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">LinkedListTest</span> {<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span>{<br>        <span class="hljs-type">var</span> <span class="hljs-variable">a</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">LinkedList</span>&lt;String&gt;();<br>        a.add(<span class="hljs-string">"Amy"</span>);<br>        a.add(<span class="hljs-string">"Carl"</span>);<br>        a.add(<span class="hljs-string">"Eric"</span>);<br><br>        <span class="hljs-type">var</span> <span class="hljs-variable">b</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">LinkedList</span>&lt;String&gt;();<br>        b.add(<span class="hljs-string">"Bob"</span>);<br>        b.add(<span class="hljs-string">"Doug"</span>);<br>        b.add(<span class="hljs-string">"Frances"</span>);<br>        b.add(<span class="hljs-string">"Gloria"</span>);<br><br>        ListIterator&lt;String&gt; aIter = a.listIterator();<br>        Iterator&lt;String&gt; bIter = b.iterator();<br><br>        <span class="hljs-comment">//将b合并到a</span><br>        <span class="hljs-keyword">while</span>(bIter.hasNext()){<br>            <span class="hljs-keyword">if</span>(aIter.hasNext()) aIter.next();<br>            aIter.add(bIter.next());<br>        }<br><br>        System.out.println(a);<br><br>        <span class="hljs-comment">//每各隔一个元素删除b中元素</span><br>        bIter = b.iterator();<br>        <span class="hljs-keyword">while</span>(bIter.hasNext()){<br>            bIter.next();<br>            <span class="hljs-keyword">if</span>(bIter.hasNext()){<br>                bIter.next();<br>                bIter.remove();<br>            }<br>        }<br>        System.out.println(b);<br>        <span class="hljs-comment">//删除a中所有包含在b中的元素</span><br>        a.removeAll(b);<br><br>        System.out.println(a);<br>    }<br>}<br></code></pre></td></tr></tbody></table></figure>

<h4 id="数组列表"><a href="#数组列表" class="headerlink" title="数组列表"></a>数组列表</h4><p>集合类库提供了一种大家熟悉的 ArrayList 类，这个类也实现了 List 接口。ArrayList 封装了一个动态再分配的对象数组。</p>
<h4 id="散列集"><a href="#散列集" class="headerlink" title="散列集"></a>散列集</h4><p> 散列表为每个对象计算一个整数 ， 称为散列码 （ hashcode ) 。 散列码是由对象的实例域产生的一个整数。 </p>
<p>Java 集合类库提供了一个 HashSet 类，它实现了基于散列表的集 。</p>
<p>可以用 add 方法添加元素。contains 方法已经被重新定义， 用来快速地查看是否某个元素已经出现在集中。它只在某个桶中査找元素，而不必查看集合中的所有元素。<br><strong>示例：</strong></p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">setTest</span> {<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span>{<br>        <span class="hljs-type">var</span> <span class="hljs-variable">words</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">HashSet</span>&lt;String&gt;();<br>        <span class="hljs-type">long</span> <span class="hljs-variable">totalTime</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>;<br><br>        <span class="hljs-keyword">try</span>(<span class="hljs-type">var</span> <span class="hljs-variable">in</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Scanner</span>(System.in))<br>        {<br>            <span class="hljs-keyword">while</span>(!in.hasNext(<span class="hljs-string">"0"</span>)){<br>                <span class="hljs-type">String</span> <span class="hljs-variable">word</span> <span class="hljs-operator">=</span> in.next();<br>                <span class="hljs-type">long</span> <span class="hljs-variable">callTime</span> <span class="hljs-operator">=</span> System.currentTimeMillis();<br>                words.add(word);<br>                callTime = System.currentTimeMillis()-callTime;<br>                totalTime+=callTime;<br>            }<br>        }<br><br>        Iterator&lt;String&gt; iter = words.iterator();<br>        <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">1</span>;i&lt;=<span class="hljs-number">20</span>&amp;&amp;iter.hasNext();i++)<br>            System.out.println(iter.next());<br>        System.out.println(<span class="hljs-string">"..."</span>);<br>        System.out.println(words.size()+<span class="hljs-string">" distinct words\t"</span>+totalTime+<span class="hljs-string">" milliseconds"</span>);<br>    }<br>}<br></code></pre></td></tr></tbody></table></figure>

<h4 id="树集"><a href="#树集" class="headerlink" title="树集"></a>树集</h4><p> 树集是一个有序集合( sorted collection ) ，可以以任意顺序将元素插入到集合中。在对集合进行遍历时，每个值将自动地按照排序后的顺序呈现。</p>
<p><strong>示例：</strong></p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">TreeSetTest</span> {<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span>{<br>        <span class="hljs-type">var</span> <span class="hljs-variable">parts</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">TreeSet</span>&lt;Item&gt;();<br>        parts.add(<span class="hljs-keyword">new</span> <span class="hljs-title class_">Item</span>(<span class="hljs-string">"Toaster"</span>,<span class="hljs-number">1234</span>));<br>        parts.add(<span class="hljs-keyword">new</span> <span class="hljs-title class_">Item</span>(<span class="hljs-string">"Widget"</span>,<span class="hljs-number">4562</span>));<br>        parts.add(<span class="hljs-keyword">new</span> <span class="hljs-title class_">Item</span>(<span class="hljs-string">"Modem"</span>,<span class="hljs-number">9912</span>));<br>        System.out.println(parts);  <span class="hljs-comment">//按照部件编号排序</span><br><br>        <span class="hljs-comment">//定制比较器，按照描述信息排序</span><br>        <span class="hljs-type">var</span> <span class="hljs-variable">sortByDescription</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">TreeSet</span>&lt;Item&gt;(Comparator.comparing(Item::getDescription));<br>        sortByDescription.addAll(parts);<br>        System.out.println(sortByDescription);<br>    }<br>}<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Item</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Comparable</span>&lt;Item&gt;{<br>    <span class="hljs-keyword">private</span> String description;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span> partNumber;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">Item</span><span class="hljs-params">(String aDescription,<span class="hljs-type">int</span> aPartNumber)</span>{<br>        description = aDescription;<br>        partNumber = aPartNumber;<br>    }<br><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">getDescription</span><span class="hljs-params">()</span>{<br>        <span class="hljs-keyword">return</span> description;<br>    }<br><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">toString</span><span class="hljs-params">()</span>{<br>        <span class="hljs-keyword">return</span> <span class="hljs-string">"[description="</span>+description+<span class="hljs-string">", partNumber="</span>+partNumber+<span class="hljs-string">"]"</span>;<br>    }<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">equals</span><span class="hljs-params">(Object otherObject)</span>{<br>        <span class="hljs-keyword">if</span>(<span class="hljs-built_in">this</span>==otherObject) <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;<br>        <span class="hljs-keyword">if</span>(otherObject==<span class="hljs-literal">null</span>) <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>        <span class="hljs-keyword">if</span>(getClass()!=otherObject.getClass()) <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>        <span class="hljs-type">var</span> <span class="hljs-variable">other</span> <span class="hljs-operator">=</span> (Item) otherObject;<br>        <span class="hljs-keyword">return</span> Objects.equals(description,other.description)&amp;&amp;partNumber == other.partNumber;<br>    }<br>    <br>    <span class="hljs-keyword">public</span> <span class="hljs-type">int</span> <span class="hljs-title function_">compareTo</span><span class="hljs-params">(Item other)</span>{<br>        <span class="hljs-comment">//实现比较器方法，按照部件编号排序</span><br>        <span class="hljs-type">int</span> <span class="hljs-variable">diff</span> <span class="hljs-operator">=</span> Integer.compare(partNumber,other.partNumber);<br>        <span class="hljs-comment">//编号相同时再比较描述语</span><br>        <span class="hljs-keyword">return</span> diff!=<span class="hljs-number">0</span>?diff:description.compareTo(other.description);<br>    }<br>}<br></code></pre></td></tr></tbody></table></figure>

<h4 id="队列与双端队列"><a href="#队列与双端队列" class="headerlink" title="队列与双端队列"></a>队列与双端队列</h4><p> 队列可以让人们有效地在尾部添加一个元素 ， 在头部删除一个元素。</p>
<p>有两个端头的队列 ， 即双端队列 ， 可以让人们有效地在头部和尾部同时添加或删除元<br>素。</p>
<h4 id="优先级队列"><a href="#优先级队列" class="headerlink" title="优先级队列"></a>优先级队列</h4><p>优先级队列 （ priority queue ) 中的元素可以按照任意的顺序插人，却总是按照排序的顺序<br>进行检索。</p>
<p>一个优先级队列既可以保存实现了 Comparable 接口的类对象 ， 也可以保存在构造器中提供的 Comparator 对象。</p>
<p><strong>示例：</strong></p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">PriorityQueueTest</span> {<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span>{<br>        <span class="hljs-type">var</span> <span class="hljs-variable">pq</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">PriorityQueue</span>&lt;LocalDate&gt;();<br>        pq.add(LocalDate.of(<span class="hljs-number">1986</span>,<span class="hljs-number">12</span>,<span class="hljs-number">9</span>));<br>        pq.add(LocalDate.of(<span class="hljs-number">1815</span>,<span class="hljs-number">12</span>,<span class="hljs-number">10</span>));<br>        pq.add(LocalDate.of(<span class="hljs-number">1903</span>,<span class="hljs-number">12</span>,<span class="hljs-number">3</span>));<br>        pq.add(LocalDate.of(<span class="hljs-number">1910</span>,<span class="hljs-number">6</span>,<span class="hljs-number">22</span>));<br><br>        <span class="hljs-comment">//遍历优先队列中的元素</span><br>        System.out.println(<span class="hljs-string">"Iterating over elements..."</span>);<br>        <span class="hljs-keyword">for</span>(LocalDate date:pq){<br>            System.out.println(date);<br>        }<br>        <span class="hljs-comment">//依次删除队列中的最小元素</span><br>        System.out.println(<span class="hljs-string">"Removing elements..."</span>);<br>        <span class="hljs-keyword">while</span>(!pq.isEmpty())<br>            System.out.println(pq.remove());<br>    }<br>}<br></code></pre></td></tr></tbody></table></figure>

<p> 这里的迭代并不是按照元素的排列顺序访问的。而删除却总是删掉剩余元素中优先级数最小的那个元素。</p>
<h3 id="映射"><a href="#映射" class="headerlink" title="映射"></a>映射</h3><p>映射用来存放键/ 值对，如果提供了键，就能够查找到值。</p>
<p>Java 类库为映射提供了两个通用的实现 ： HashMap 和 TreeMap。这两个类都实现了 Map 接口。</p>
<p>每当往映射中添加对象时 ， 必须同时提供一个键。</p>
<p>要想检索一个对象，必须使用（ 因而 ， 必须记住 ）一个键 。</p>
<p><strong>示例：</strong></p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">MapTest</span> {<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span>{<br>        <span class="hljs-type">var</span> <span class="hljs-variable">staff</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">HashMap</span>&lt;String,String&gt;();<br>        staff.put(<span class="hljs-string">"first"</span>,<span class="hljs-string">"Amy Lee"</span>);<br>        staff.put(<span class="hljs-string">"second"</span>,<span class="hljs-string">"Harry Hacker"</span>);<br>        staff.put(<span class="hljs-string">"third"</span>,<span class="hljs-string">"Gary Cooper"</span>);<br>        staff.put(<span class="hljs-string">"forth"</span>,<span class="hljs-string">"Francesca Cruz"</span>);<br><br>        <span class="hljs-comment">//输出所有的元素</span><br>        System.out.println(staff);<br><br>        <span class="hljs-comment">//删除一个元素</span><br>        staff.remove(<span class="hljs-string">"second"</span>);<br><br>        <span class="hljs-comment">//更换一个元素</span><br>        staff.put(<span class="hljs-string">"forth"</span>,<span class="hljs-string">"Francesca Miller"</span>);<br><br>        <span class="hljs-comment">//查看一个元素</span><br>        System.out.println(staff.get(<span class="hljs-string">"third"</span>));<br><br>        <span class="hljs-comment">//遍历所有的元素</span><br>        staff.forEach((k,v)-&gt;<br>                System.out.println(<span class="hljs-string">"key="</span> + k + <span class="hljs-string">", value="</span> + v));<br>    }<br>}<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Employee</span> {<br>    <span class="hljs-keyword">private</span> String name;<br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">Employee</span><span class="hljs-params">(String name)</span>{<br>        <span class="hljs-built_in">this</span>.name = name;<br>    }<br><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">getName</span><span class="hljs-params">()</span>{<br>        <span class="hljs-keyword">return</span> name;<br>    }<br>}<br></code></pre></td></tr></tbody></table></figure>

<h4 id="映射视图"><a href="#映射视图" class="headerlink" title="映射视图"></a>映射视图</h4><p>有 3 种视图 ： 键集 、 值集合 （ 不是一个集 ） 以及键 / 值对集 。 键和键 / 值对可以构成一个<br>集，因为映射中一个键只能有一个副本。</p>
<p>分别返回这 3 个视图</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java">Set&lt;K&gt; <span class="hljs-title function_">keySet</span> <span class="hljs-params">()</span><br>Collection&lt;V&gt; values <span class="hljs-number">0</span><br>Set&lt;Map.Entry&lt;K,V&gt; entrySetO<br></code></pre></td></tr></tbody></table></figure>

<h3 id="算-法"><a href="#算-法" class="headerlink" title="算 法"></a>算 法</h3><p><strong>排序与混排</strong></p>
<p>Collections 类中的 sort 方法可以对实现了 List 接口的集合进行排序。</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java">List&lt;String&gt; staff = <span class="hljs-keyword">new</span> <span class="hljs-title class_">LinkedList</span>&lt;&gt;();<br>fill collection<br>Collections.sort(staff) <br></code></pre></td></tr></tbody></table></figure>

<p>这个方法假定列表元素实现了 Comparable 接口。如果想采用其他方式对列表进行排序 ， 可以使用 List 接口的 sort 方法并传入一个 Comparator 对象。</p>
<p>Collections 类有一个算法 shuffle , 其功能与排序刚好相反 ， 即随机地混排列表中元素的<br>顺序。</p>
<p><strong>示例：</strong></p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ShuffleTest</span> {<br>    <span class="hljs-keyword">public</span>  <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span>{<br>        <span class="hljs-type">var</span> <span class="hljs-variable">numbers</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;Integer&gt;();<br>        <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">0</span>;i&lt;<span class="hljs-number">49</span>;i++)<br>            numbers.add(i);<br>        <span class="hljs-comment">//随机打乱列表</span><br>        Collections.shuffle(numbers);<br>        <span class="hljs-comment">//取出列表前6个值</span><br>        List&lt;Integer&gt; winningCombination = numbers.subList(<span class="hljs-number">0</span>,<span class="hljs-number">6</span>);<br>        <span class="hljs-comment">//排序</span><br>        Collections.sort(winningCombination);<br>        System.out.println(winningCombination);<br>    }<br>}<br></code></pre></td></tr></tbody></table></figure>

<h4 id="二分查找"><a href="#二分查找" class="headerlink" title="二分查找"></a>二分查找</h4><figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java">i = Collections.binarySearch(c,element);<br>i = Collections.binarySearch(c,element,comparator);<br></code></pre></td></tr></tbody></table></figure>

<h4 id="批操作"><a href="#批操作" class="headerlink" title="批操作"></a>批操作</h4><figure class="highlight reasonml"><table><tbody><tr><td class="code"><pre><code class="hljs reasonml">coll1.remove<span class="hljs-constructor">All(<span class="hljs-params">coll2</span>)</span>;	<span class="hljs-comment">//从 colli 中删除 ) 112 中出现的所有元素</span><br>coll1.retain<span class="hljs-constructor">All(<span class="hljs-params">coll2</span>)</span>;	<span class="hljs-comment">//从 colli 中删除所有未在 C 0112 中出现的元素</span><br></code></pre></td></tr></tbody></table></figure>

<h4 id="集合与数组的转换"><a href="#集合与数组的转换" class="headerlink" title="集合与数组的转换"></a>集合与数组的转换</h4><p>把一个数组转换为集合</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java">String[] values = ...;<br>HashSet&lt;String&gt; staff = <span class="hljs-keyword">new</span> <span class="hljs-title class_">HashSet</span>&lt;&gt;(Arrays.asList(values))<br></code></pre></td></tr></tbody></table></figure>

<p>从集合得到数组</p>
<figure class="highlight arduino"><table><tbody><tr><td class="code"><pre><code class="hljs arduino"><span class="hljs-type">String</span>[] values = staff.<span class="hljs-built_in">toArray</span>(<span class="hljs-keyword">new</span> <span class="hljs-type">String</span>[<span class="hljs-number">0</span>]);<br></code></pre></td></tr></tbody></table></figure>

<h3 id="遗留的集合"><a href="#遗留的集合" class="headerlink" title="遗留的集合"></a>遗留的集合</h3><img src="https://pic.imgdb.cn/item/638b72ac16f2c2beb1616060.png" style="zoom:80%;">

<h4 id="Hashtable类"><a href="#Hashtable类" class="headerlink" title="Hashtable类"></a>Hashtable类</h4><p>Hashtable 类与 HashMap 类的作用一样 ， 实际上，它们拥有相同的接口 。</p>
<h4 id="枚举"><a href="#枚举" class="headerlink" title="枚举"></a>枚举</h4><p>遗留集合使用 Enumeration 接口对元素序列进行遍历 。 Enumeration 接口有两个方法 ， 即<br>hasMoreElements 和 nextElement。</p>
<h4 id="属性映射"><a href="#属性映射" class="headerlink" title="属性映射"></a>属性映射</h4><p>属性映射（ property map ) 是一个类型非常特殊的映射结构。它有下面 3 个特性 ：<br>• 键与值都是字符串 。<br>• 表可以保存到一个文件中 ，也可以从文件中加载。<br>• 使用一个默认的辅助表 。<br>实现属性映射的 Java 平台类称为 Properties。</p>
<h4 id="栈"><a href="#栈" class="headerlink" title="栈"></a>栈</h4><figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java">E <span class="hljs-title function_">push</span><span class="hljs-params">(E item)</span><br>E <span class="hljs-title function_">pop</span><span class="hljs-params">()</span><br>E <span class="hljs-title function_">peek</span><span class="hljs-params">()</span><br></code></pre></td></tr></tbody></table></figure>

<h4 id="位集"><a href="#位集" class="headerlink" title="位集"></a>位集</h4><p> BitSet 类用于存放一个位序列，如果需要高效地存储位序列（例如，标志 ）就可以使用位集。</p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>Gson解析</title>
    <url>/2022/11/29/Gson%E8%A7%A3%E6%9E%90/</url>
    <content><![CDATA[<p>Gson是google发布的library，主要为了方便将Java对象序列化Serialization至轻量化的封包格式JSON，提供了很多方便快捷的方法。 </p>
<p><strong>通过gradle导入包</strong></p>
<figure class="highlight groovy"><table><tbody><tr><td class="code"><pre><code class="hljs groovy">implementation <span class="hljs-string">'com.google.code.gson:gson:2.10'</span><br></code></pre></td></tr></tbody></table></figure>

<h3 id="Gson基本用法"><a href="#Gson基本用法" class="headerlink" title="Gson基本用法"></a>Gson基本用法</h3><p>Gson提供了两个方法直接用于解析和生成方法，二者都有重载方法：</p>
<ul>
<li><code>fromJson()</code>：实现反序列化</li>
<li><code>toJson()</code>：实现序列化</li>
</ul>
<h4 id="基本数据类型的生成"><a href="#基本数据类型的生成" class="headerlink" title="基本数据类型的生成"></a><strong>基本数据类型的生成</strong></h4><figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-type">Gson</span> <span class="hljs-variable">gson</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Gson</span>();<br><span class="hljs-type">String</span> <span class="hljs-variable">jsonNumber</span> <span class="hljs-operator">=</span> gson.toJson(<span class="hljs-number">100</span>); <span class="hljs-comment">// 100</span><br><span class="hljs-type">String</span> <span class="hljs-variable">jsonBoolean</span> <span class="hljs-operator">=</span> gson.toJson(<span class="hljs-literal">false</span>); <span class="hljs-comment">// false</span><br><span class="hljs-type">String</span> <span class="hljs-variable">jsonString</span> <span class="hljs-operator">=</span> gson.toJson(<span class="hljs-string">"String"</span>); <span class="hljs-comment">//"String"</span><br></code></pre></td></tr></tbody></table></figure>

<h4 id="POJO类的生成与解析"><a href="#POJO类的生成与解析" class="headerlink" title="POJO类的生成与解析"></a>POJO类的生成与解析</h4><p>对于普通java类</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">User</span> {<br>   <span class="hljs-keyword">public</span> String name; <br>   <span class="hljs-keyword">public</span> <span class="hljs-type">int</span> age; <br>}<br></code></pre></td></tr></tbody></table></figure>

<p> <strong>生成JSON</strong> </p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-type">Gson</span> <span class="hljs-variable">gson</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Gson</span>();<br><span class="hljs-type">User</span> <span class="hljs-variable">user</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">User</span>(<span class="hljs-string">"Sunny"</span>,<span class="hljs-number">24</span>);<br><span class="hljs-type">String</span> <span class="hljs-variable">jsonObject</span> <span class="hljs-operator">=</span> gson.toJson(user); <span class="hljs-comment">// {"name":"Sunny","age":24}</span><br></code></pre></td></tr></tbody></table></figure>

<p> <strong>解析JSON</strong> </p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-type">Gson</span> <span class="hljs-variable">gson</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Gson</span>();<br><span class="hljs-type">String</span> <span class="hljs-variable">jsonString</span> <span class="hljs-operator">=</span> <span class="hljs-string">"{\"name\":\"sunny\",\"age\":24}"</span>;<br><span class="hljs-type">User</span> <span class="hljs-variable">user</span> <span class="hljs-operator">=</span> gson.fromJson(jsonString, User.class);<br></code></pre></td></tr></tbody></table></figure>

<h3 id="Gson中的泛型"><a href="#Gson中的泛型" class="headerlink" title="Gson中的泛型"></a>Gson中的泛型</h3><p> 当我们解析Json数组时，一般有两种方式：使用数组，使用List。 </p>
<h5 id="数组"><a href="#数组" class="headerlink" title="数组"></a>数组</h5><figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java">String[] strings = gson.fromJson(jsonArray, String[].class);<br></code></pre></td></tr></tbody></table></figure>

<h5 id="List"><a href="#List" class="headerlink" title="List"></a>List</h5><figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java">List&lt;String&gt; stringList = gson.fromJson(jsonArray, <span class="hljs-keyword">new</span> <span class="hljs-title class_">TypeToken</span>&lt;List&lt;String&gt;&gt;() {}.getType());<br></code></pre></td></tr></tbody></table></figure>

<p>TypeToken 这个类帮助我们捕获（capture）像 List 这样的泛型信息。Java编译器会把捕获到的泛型信息编译到这个匿名内部类里，然后在运行时就可以被 getType() 方法用反射的 API 提取到。也就是将泛型T转成 .class。</p>
<h3 id="注解的使用"><a href="#注解的使用" class="headerlink" title="注解的使用"></a>注解的使用</h3><p>@Expose注解：区分实体中不想被序列化的属性，其自身包含两个属性deserialize(反序列化)和serialize(序列化)，默认都为true</p>
<p>@SerializeName注解：定义属性序列化后的名称</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">UserBean</span> {<br>      <span class="hljs-meta">@SerializeName("user_name")</span><br>      <span class="hljs-keyword">private</span> String userName;<br>      <span class="hljs-meta">@SerializeName("user_age")</span><br>      <span class="hljs-keyword">private</span> String userAge;<br>}<br></code></pre></td></tr></tbody></table></figure>

<h3 id="Gson解析复杂Json数据"><a href="#Gson解析复杂Json数据" class="headerlink" title="Gson解析复杂Json数据"></a>Gson解析复杂Json数据</h3><p>首先，要知道两个类：<br> <strong>1、JsonParse</strong><br> 从名称我们就可以看出，这是一个解析类。没错，它可以把 JSON 数据分别通过<code>getAsJsonObject</code>和<code>getAsJsonArray</code>解析成<code>JsonObject</code>和<code>JsonArray</code>。这跟普通的解析 JSON 差不多。<br> <strong>2、JsonElement</strong><br> 是一个抽象类，代表 JSON 串中的某一个元素，可以是<code>JsonObject/JsonArray/JsonPrimitive/...</code>中的任何一种元素。</p>
<h5 id="没有数据头的纯数组"><a href="#没有数据头的纯数组" class="headerlink" title="没有数据头的纯数组"></a>没有数据头的纯数组</h5><figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java">[<br>  {<br>    <span class="hljs-string">"name"</span>: <span class="hljs-string">"zhangsan"</span>,<br>    <span class="hljs-string">"age"</span>: <span class="hljs-string">"10"</span>,<br>    <span class="hljs-string">"phone"</span>: <span class="hljs-string">"11111"</span>,<br>    <span class="hljs-string">"email"</span>: <span class="hljs-string">"11111@11.com"</span><br>  },<br>  {<br>    <span class="hljs-string">"name"</span>: <span class="hljs-string">"lisi"</span>,<br>    <span class="hljs-string">"age"</span>: <span class="hljs-string">"20"</span>,<br>    <span class="hljs-string">"phone"</span>: <span class="hljs-string">"22222"</span>,<br>    <span class="hljs-string">"email"</span>: <span class="hljs-string">"22222@22.com"</span><br>  },<br>  ...<br>]<br></code></pre></td></tr></tbody></table></figure>

<p> 开始解析，首先定义一个用户类： </p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">UserBean</span> {<br>    <span class="hljs-comment">//变量名跟JSON数据的字段名需要一致</span><br>    <span class="hljs-keyword">private</span> String name ;<br>    <span class="hljs-keyword">private</span> String age;<br>    <span class="hljs-keyword">private</span> String phone;<br>    <span class="hljs-keyword">private</span> String email;<br>    ...<br>}<br></code></pre></td></tr></tbody></table></figure>

<p> 方法一：Gson可以直接解析成一个List </p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java">List&lt;UserBean&gt; userList = gson.fromJson(jsonStr, <span class="hljs-keyword">new</span> <span class="hljs-title class_">TypeToken</span>&lt;List&lt;UserBean&gt;&gt;(){}.getType());<br></code></pre></td></tr></tbody></table></figure>

<p> 方法二：传统老实方法 </p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">//Json的解析类对象</span><br><span class="hljs-type">JsonParser</span> <span class="hljs-variable">parser</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">JsonParser</span>();<br><span class="hljs-comment">//将JSON的String 转成一个JsonArray对象</span><br><span class="hljs-type">JsonArray</span> <span class="hljs-variable">jsonArray</span> <span class="hljs-operator">=</span> parser.parse(strByJson).getAsJsonArray();<br><br><span class="hljs-type">Gson</span> <span class="hljs-variable">gson</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Gson</span>();<br>ArrayList&lt;UserBean&gt; userBeanList = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;&gt;();<br><br><span class="hljs-comment">//加强for循环遍历JsonArray</span><br><span class="hljs-keyword">for</span> (JsonElement user : jsonArray) {<br>    <span class="hljs-comment">//使用GSON，直接转成Bean对象</span><br>    <span class="hljs-type">UserBean</span> <span class="hljs-variable">userBean</span> <span class="hljs-operator">=</span> gson.fromJson(user, UserBean.class);<br>    userBeanList.add(userBean);<br>}<br></code></pre></td></tr></tbody></table></figure>

<h5 id="有数据头的复杂数据"><a href="#有数据头的复杂数据" class="headerlink" title="有数据头的复杂数据"></a><strong>有数据头的复杂数据</strong></h5><p><img src="https://pic.imgdb.cn/item/638594f016f2c2beb11c9249.png"></p>
<p> 方法一：Gson可以直接解析成一个List </p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-type">JSONObject</span> <span class="hljs-variable">photosJsonObject</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">JSONObject</span>(jsonString).getJSONObject(<span class="hljs-string">"photos"</span>);<br><span class="hljs-type">ResultBean</span> <span class="hljs-variable">result</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Gson</span>()<br>    .fromJson(String.valueOf(photosJsonObject),ResultBean.class);<br>items = result.getPhotos();<br></code></pre></td></tr></tbody></table></figure>

<p> 方法二：传统老实方法 </p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-type">JSONObject</span> <span class="hljs-variable">photosJsonObject</span> <span class="hljs-operator">=</span> jsonBody.getJSONObject(<span class="hljs-string">"photos"</span>);<br><span class="hljs-type">JSONArray</span> <span class="hljs-variable">photoJsonArray</span> <span class="hljs-operator">=</span> photosJsonObject.getJSONArray(<span class="hljs-string">"photo"</span>);<br><span class="hljs-comment">//取出每张图片的信息 生成一个个GalleryItem对象</span><br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">0</span>;i&lt;photoJsonArray.length();i++){<br>    <span class="hljs-type">JSONObject</span> <span class="hljs-variable">photoJsonObject</span> <span class="hljs-operator">=</span> photoJsonArray.getJSONObject(i);<br><br>    <span class="hljs-type">GalleryItem</span> <span class="hljs-variable">item</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">GalleryItem</span>();<br>    item.setId(photoJsonObject.getString(<span class="hljs-string">"id"</span>));<br>    item.setCaption(photoJsonObject.getString(<span class="hljs-string">"title"</span>));<br><br>    <span class="hljs-comment">//并不是每张图片都含有usr_s链接</span><br>    <span class="hljs-keyword">if</span>(!photoJsonObject.has(<span class="hljs-string">"url_s"</span>)) <span class="hljs-keyword">continue</span>;<br><br>    item.setUrl(photoJsonObject.getString(<span class="hljs-string">"url_s"</span>));<br>    items.add(item);<br>}<br></code></pre></td></tr></tbody></table></figure>

<p>参考资料：</p>
<p> <a href="https://www.jianshu.com/p/886f7b7fca7d/">Android：Gson解析 - 从简单数据到复杂数据</a> </p>
]]></content>
      <tags>
        <tag>Android</tag>
      </tags>
  </entry>
  <entry>
    <title>继承</title>
    <url>/2022/11/28/%E7%BB%A7%E6%89%BF/</url>
    <content><![CDATA[<h1 id="继承"><a href="#继承" class="headerlink" title="继承"></a>继承</h1><h3 id="继承的概念"><a href="#继承的概念" class="headerlink" title="继承的概念"></a>继承的概念</h3><p> 继承是java面向对象编程技术的一块基石，因为它允许创建分等级层次的类。 </p>
<p> 利用继承，人们可以基于已存在的类构造一个新类。继承已存在的类就是复用 （ 继承 ）这些类的方法和域。在此基础上，还可以添加一些新的方法和域，以满足新的需求。</p>
<p><strong>生活中的继承</strong></p>
<p><img src="https://pic.imgdb.cn/item/6381f5b716f2c2beb1d7587f.png"></p>
<p>兔子和羊属于食草动物类，狮子和豹属于食肉动物类。</p>
<p>食草动物和食肉动物又是属于动物类。</p>
<p>所以继承需要符合的关系是：is-a，父类更通用，子类更具体。</p>
<p>虽然食草动物和食肉动物都是属于动物，但是两者的属性和行为上有差别，所以子类会具有父类的一般特性也会具有自身的特性。</p>
<p><strong>类的继承格式</strong></p>
<p> 在 Java 中通过 extends 关键字可以申明一个类是从另外一个类继承而来的，一般形式如下： </p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Superclass</span>{<br>}<br><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">childClass</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Superclass</span>{<br>}<br></code></pre></td></tr></tbody></table></figure>

<p><strong>为什么需要继承</strong></p>
<p>接下来我们通过示例来说明这个需求。</p>
<p>开发动物类，其中动物分别为企鹅以及老鼠，要求如下：</p>
<ul>
<li>兔子：属性（姓名，id），方法（吃，睡，打洞）</li>
<li>老虎：属性（姓名，id），方法（吃，睡，捕猎）</li>
</ul>
<p>兔子类：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Rabbit</span> { <br>    <span class="hljs-keyword">private</span> String name; <br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span> id; <br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">Rabbit</span><span class="hljs-params">(String myName, <span class="hljs-type">int</span>  myid)</span> { <br>        name = myName; <br>        id = myid; <br>    } <br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">eat</span><span class="hljs-params">()</span>{ <br>        System.out.println(name+<span class="hljs-string">"正在吃"</span>); <br>    }<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">sleep</span><span class="hljs-params">()</span>{<br>        System.out.println(name+<span class="hljs-string">"正在睡"</span>);<br>    }<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">dig</span><span class="hljs-params">()</span> { <br>        System.out.println(name+<span class="hljs-string">"正在打洞"</span>); <br>    } <br>}<br></code></pre></td></tr></tbody></table></figure>

<p>老虎类：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Tiger</span> { <br>    <span class="hljs-keyword">private</span> String name; <br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span> id; <br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">Tiger</span><span class="hljs-params">(String myName, <span class="hljs-type">int</span>  myid)</span> { <br>        name = myName; <br>        id = myid; <br>    } <br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">eat</span><span class="hljs-params">()</span>{ <br>        System.out.println(name+<span class="hljs-string">"正在吃"</span>); <br>    }<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">sleep</span><span class="hljs-params">()</span>{<br>        System.out.println(name+<span class="hljs-string">"正在睡"</span>);<br>    }<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">hunt</span><span class="hljs-params">(String other)</span> { <br>        System.out.println(name+<span class="hljs-string">"正在捕捉"</span>+other); <br>    } <br>}<br></code></pre></td></tr></tbody></table></figure>

<p>从这两段代码可以看出来，代码存在重复了，导致后果就是代码量大且臃肿，而且维护性不高(维护性主要是后期需要修改的时候，就需要修改很多的代码，容易出错)，所以要从根本上解决这两段代码的问题，就需要继承，将两段代码中相同的部分提取出来组成一个父类： </p>
<p>公共父类：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Animal</span> { <br>    <span class="hljs-keyword">private</span> String name;  <br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span> id; <br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">Animal</span><span class="hljs-params">(String myName, <span class="hljs-type">int</span> myid)</span> { <br>        name = myName; <br>        id = myid;<br>    } <br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">eat</span><span class="hljs-params">()</span>{ <br>        System.out.println(name+<span class="hljs-string">"正在吃"</span>); <br>    }<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">sleep</span><span class="hljs-params">()</span>{<br>        System.out.println(name+<span class="hljs-string">"正在睡"</span>);<br>    }<br>}<br></code></pre></td></tr></tbody></table></figure>

<p>这个Animal类就可以作为一个父类，然后兔子类和老虎类继承这个类之后，就具有父类当中的属性和方法，子类就不会存在重复的代码，维护性也提高，代码也更加简洁，提高代码的复用性（复用性主要是可以多次使用，不用再多次写同样的代码），继承之后的代码： </p>
<p>兔子类：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Rabbbit</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Animal</span> { <br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">Rabbbit</span><span class="hljs-params">(String myName, <span class="hljs-type">int</span> myid)</span> { <br>        <span class="hljs-built_in">super</span>(myName, myid); <br>    } <br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">dig</span><span class="hljs-params">()</span> { <br>        System.out.println(name+<span class="hljs-string">"正在打洞"</span>); <br>    } <br>}<br></code></pre></td></tr></tbody></table></figure>

<p>老虎类：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Tiger</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Animal</span>{ <br>    <span class="hljs-keyword">private</span> String name; <br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span> id; <br>	<span class="hljs-keyword">public</span> <span class="hljs-title function_">Tiger</span><span class="hljs-params">(String myName, <span class="hljs-type">int</span> myid)</span> { <br>        <span class="hljs-built_in">super</span>(myName, myid); <br>    } <br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">hunt</span><span class="hljs-params">(String other)</span> { <br>        System.out.println(name+<span class="hljs-string">"正在捕捉"</span>+other); <br>    } <br>}<br></code></pre></td></tr></tbody></table></figure>

<p><strong>继承类型</strong></p>
<p>需要注意的是 Java 不支持多继承，但支持多重继承。</p>
<ul>
<li>多重继承</li>
</ul>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">A</span>{...}<br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">B</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">A</span>{...}<br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">C</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">B</span>{...}<br></code></pre></td></tr></tbody></table></figure>

<ul>
<li>多继承（不支持）</li>
</ul>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">A</span>{...}<br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">B</span>{...}<br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">C</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">A</span>,B{...}<br></code></pre></td></tr></tbody></table></figure>

<h3 id="继承的特性"><a href="#继承的特性" class="headerlink" title="继承的特性"></a>继承的特性</h3><ul>
<li>子类拥有父类非 private 的属性、方法。</li>
<li>子类可以拥有自己的属性和方法，即子类可以对父类进行扩展。</li>
<li>子类可以用自己的方式实现父类的方法。</li>
<li>Java 的继承是单继承，但是可以多重继承，单继承就是一个子类只能继承一个父类，多重继承就是，例如 B 类继承 A 类，C 类继承 B 类，所以按照关系就是 B 类是 C 类的父类，A 类是 B 类的父类，这是 Java 继承区别于 C++ 继承的一个特性。</li>
<li>提高了类之间的耦合性（继承的缺点，耦合度高就会造成代码之间的联系越紧密，代码独立性越差）。</li>
</ul>
<h3 id="子类构造器"><a href="#子类构造器" class="headerlink" title="子类构造器"></a>子类构造器</h3><p>子类是不继承父类的构造器（构造方法或者构造函数）的，它只是调用（隐式或显式）。如果父类的构造器带有参数，则必须在子类的构造器中显式地通过 <strong>super</strong> 关键字调用父类的构造器并配以适当的参数列表。</p>
<p>如果父类构造器没有参数，则在子类的构造器中不需要使用 <strong>super</strong> 关键字调用父类构造器，系统会自动调用父类的无参构造器。</p>
<p>实例：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">SuperClass</span> {<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span> n;<br>    SuperClass(){<br>        System.out.println(<span class="hljs-string">"SuperClass()"</span>);<br>    }<br>    SuperClass(<span class="hljs-type">int</span> n) {<br>        System.out.println(<span class="hljs-string">"SuperClass(int n)"</span>);<br>        <span class="hljs-built_in">this</span>.n = n;<br>    }<br>}<br><span class="hljs-comment">// SubClass 类继承</span><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">SubClass</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">SuperClass</span>{<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span> n;<br><br>    SubClass(){ <span class="hljs-comment">// 自动调用父类的无参数构造器</span><br>        System.out.println(<span class="hljs-string">"SubClass"</span>);<br>    }  <br><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">SubClass</span><span class="hljs-params">(<span class="hljs-type">int</span> n)</span>{ <br>        <span class="hljs-built_in">super</span>(<span class="hljs-number">300</span>);  <span class="hljs-comment">// 调用父类中带有参数的构造器</span><br>        System.out.println(<span class="hljs-string">"SubClass(int n):"</span>+n);<br>        <span class="hljs-built_in">this</span>.n = n;<br>    }<br>}<br><span class="hljs-comment">// SubClass2 类继承</span><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">SubClass2</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">SuperClass</span>{<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span> n;<br><br>    SubClass2(){<br>        <span class="hljs-built_in">super</span>(<span class="hljs-number">300</span>);  <span class="hljs-comment">// 调用父类中带有参数的构造器</span><br>        System.out.println(<span class="hljs-string">"SubClass2"</span>);<br>    }  <br><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">SubClass2</span><span class="hljs-params">(<span class="hljs-type">int</span> n)</span>{ <span class="hljs-comment">// 自动调用父类的无参数构造器</span><br>        System.out.println(<span class="hljs-string">"SubClass2(int n):"</span>+n);<br>        <span class="hljs-built_in">this</span>.n = n;<br>    }<br>}<br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">TestSuperSub</span>{<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span> <span class="hljs-params">(String args[])</span>{<br>        System.out.println(<span class="hljs-string">"------SubClass 类继承------"</span>);<br>        <span class="hljs-type">SubClass</span> <span class="hljs-variable">sc1</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">SubClass</span>();<br>        <span class="hljs-type">SubClass</span> <span class="hljs-variable">sc2</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">SubClass</span>(<span class="hljs-number">100</span>); <br>        System.out.println(<span class="hljs-string">"------SubClass2 类继承------"</span>);<br>        <span class="hljs-type">SubClass2</span> <span class="hljs-variable">sc3</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">SubClass2</span>();<br>        <span class="hljs-type">SubClass2</span> <span class="hljs-variable">sc4</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">SubClass2</span>(<span class="hljs-number">200</span>); <br>    }<br>}<br></code></pre></td></tr></tbody></table></figure>

<h3 id="重写"><a href="#重写" class="headerlink" title="重写"></a>重写</h3><p>重写是子类对父类的允许访问的方法的实现过程进行重新编写, 返回值和形参都不能改变。<strong>即外壳不变，核心重写！</strong></p>
<p>重写的好处在于子类可以根据需要，定义特定于自己的行为。 也就是说子类能够根据需要实现父类的方法。</p>
<p>实例：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Animal</span>{<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">move</span><span class="hljs-params">()</span>{<br>        System.out.println(<span class="hljs-string">"动物可以移动"</span>);<br>    }<br>}<br><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Dog</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Animal</span>{<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">move</span><span class="hljs-params">()</span>{<br>        System.out.println(<span class="hljs-string">"狗可以跑和走"</span>);<br>    }<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String args[])</span>{<br>        <span class="hljs-type">Animal</span> <span class="hljs-variable">a</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Animal</span>(); <span class="hljs-comment">// Animal 对象</span><br>        <span class="hljs-type">Animal</span> <span class="hljs-variable">b</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Dog</span>(); <span class="hljs-comment">// Dog 对象</span><br><br>        a.move();<span class="hljs-comment">//执行 Animal 类的方法</span><br><br>        b.move();<span class="hljs-comment">//执行 Dog 类的方法</span><br>    }<br>}<br></code></pre></td></tr></tbody></table></figure>

<h3 id="受保护访问"><a href="#受保护访问" class="headerlink" title="受保护访问"></a>受保护访问</h3><p>大家都知道， 最好将类中的域标记为 private ,而方法标记为 public。任何声明为private的内容对其他类都是不可见的，子类也不可见。</p>
<p>然而， 在有些时候，人们希望超类中的某些方法允许被子类访问 ，或允许子类的方法访问超类的某个域。为此，需要将这些方法或域声明为protected。例如 ， 如果将超类Animal中的 name 声明为 proteced , 而不是私有的，rabbit类和tiger类中的方法就可以直接地访问它，而Animal类的对象却不能直接访问 。</p>
<p>下面归纳一下 Java 用于控制可见性的 4 个访问修饰符 ：</p>
<ul>
<li>仅对本类可见——private</li>
<li>对所有类可见——public </li>
<li>对本包和所有子类可见——protected</li>
<li>对本包可见——默认</li>
</ul>
<h3 id="继承关键字"><a href="#继承关键字" class="headerlink" title="继承关键字"></a>继承关键字</h3><p><strong>super和this关键字</strong></p>
<p>super关键字：我们可以通过super关键字来实现对父类成员的访问，用来引用当前对象的父类。</p>
<p>this关键字：指向自己的引用。</p>
<p>实例：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Animal</span> {<br>    <span class="hljs-keyword">void</span> <span class="hljs-title function_">eat</span><span class="hljs-params">()</span> {<br>        System.out.println(<span class="hljs-string">"animal : eat"</span>);<br>    }<br>}<br><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Dog</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Animal</span> {<br>    <span class="hljs-keyword">void</span> <span class="hljs-title function_">eat</span><span class="hljs-params">()</span> {<br>        System.out.println(<span class="hljs-string">"dog : eat"</span>);<br>    }<br>    <span class="hljs-keyword">void</span> <span class="hljs-title function_">eatTest</span><span class="hljs-params">()</span> {<br>        <span class="hljs-built_in">this</span>.eat();   <span class="hljs-comment">// this 调用自己的方法</span><br>        <span class="hljs-built_in">super</span>.eat();  <span class="hljs-comment">// super 调用父类方法</span><br>    }<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> {<br>        <span class="hljs-type">Animal</span> <span class="hljs-variable">a</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Animal</span>();<br>        a.eat();<br>        <span class="hljs-type">Dog</span> <span class="hljs-variable">d</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Dog</span>();<br>        d.eatTest();<br>    }<br>}<br></code></pre></td></tr></tbody></table></figure>

<p><strong>final 关键字</strong></p>
<p>final 可以用来修饰变量（包括类属性、对象属性、局部变量和形参）、方法（包括类方法和对象方法）和类。</p>
<p>final 含义为 “最终的”。</p>
<p>使用 final 关键字声明类，就是把类定义定义为最终类，不能被继承，或者用于修饰方法，该方法不能被子类重写：</p>
<ul>
<li>声明类：</li>
</ul>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">final</span> class 类名{<br>}<br></code></pre></td></tr></tbody></table></figure>

<ul>
<li>声明方法</li>
</ul>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java">(<span class="hljs-keyword">public</span>/<span class="hljs-keyword">private</span>/<span class="hljs-keyword">default</span>/<span class="hljs-keyword">protected</span>) <span class="hljs-keyword">final</span> 返回值类型 方法名（）{<br>}<br></code></pre></td></tr></tbody></table></figure>

<p> <strong>注：</strong> final 定义的类，其中的属性、方法不是 final 的。 </p>
]]></content>
      <tags>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>Git使用教程</title>
    <url>/2022/11/27/Git%E4%BD%BF%E7%94%A8%E6%95%99%E7%A8%8B/</url>
    <content><![CDATA[<h3 id="Git使用教程"><a href="#Git使用教程" class="headerlink" title="Git使用教程"></a>Git使用教程</h3><p><strong>Git是什么</strong></p>
<p>Git是目前世界上最先进的分布式版本控制系统。<br>工作原理 / 流程： </p>
<p><img src="https://pic.imgdb.cn/item/6381f60616f2c2beb1d7b331.png"></p>
<p>Workspace：工作区<br>Index / Stage：暂存区<br>Repository：仓库区（或本地仓库）<br>Remote：远程仓库 </p>
<p><strong>安装Git</strong> </p>
<p>首先在官网下载Git，默认安装完成后，在开始菜单找到“Git –&gt; Git Bash”,打开后输入</p>
<figure class="highlight stylus"><table><tbody><tr><td class="code"><pre><code class="hljs stylus">git config <span class="hljs-attr">--global</span> user<span class="hljs-selector-class">.name</span> <span class="hljs-string">"youusername"</span>  <span class="hljs-comment">//github仓库的用户名</span><br>git config <span class="hljs-attr">--global</span> user<span class="hljs-selector-class">.email</span> <span class="hljs-string">"youemail"</span>	<span class="hljs-comment">//注册github仓库的邮箱</span><br></code></pre></td></tr></tbody></table></figure>

<p>因为Git是分布式版本控制系统，所以需要填写用户名和邮箱作为一个标识。</p>
<p>注意：git config –global 参数，有了这个参数，表示你这台机器上所有的Git仓库都会使用这个配置，当然你也可以对某个仓库指定的不同的用户名和邮箱。</p>
<h4 id="一、创建版本库"><a href="#一、创建版本库" class="headerlink" title="一、创建版本库"></a>一、创建版本库</h4><p>什么是版本库？版本库又名仓库，英文名repository,你可以简单的理解一个目录，这个目录里面的所有文件都可以被Git管理起来 。</p>
<p>在任何一个文件夹打开Git bash, 创建一个版本库</p>
<figure class="highlight c"><table><tbody><tr><td class="code"><pre><code class="hljs c">mkdir testgit<br>cd testgit<br>pwd	<span class="hljs-comment">//显示当前目录</span><br></code></pre></td></tr></tbody></table></figure>

<p>创建完后可以在 testgit 目录下新建一个文件 test.txt, 然后可以使用一下命令提交文件到仓库</p>
<p>1.初始化仓库</p>
<p><code>git init</code></p>
<p>2.将文件添加到暂存区</p>
<p><code>git add test.txt</code> 	</p>
<p>3.把文件提交到仓库</p>
<p><code>git commit -m "first commit"</code></p>
<p>提交后可以查看是否还有文件未提交：<code>git status</code></p>
<p>也可以修改test文件后查看修改内容：<code>git diff test.txt</code></p>
<h4 id="二、版本回退"><a href="#二、版本回退" class="headerlink" title="二、版本回退"></a>二、版本回退</h4><p>查看文件修改历史记录：<code>git log</code></p>
<p>如果嫌显示信息太多，也可以使用命令 <code>git log -pretty=online</code></p>
<p>版本回退命令：</p>
<p>1.回退到上一个版本</p>
<p><code>git reset --hard HEAD^</code></p>
<p>2.回退到上上一个版本</p>
<p><code>git reset --hard HEAD^^</code></p>
<p>3.回退到前100个版本</p>
<p><code>git reset --hard HEAD~100</code></p>
<p>4.回退到特定版本</p>
<p><code>git reset --hard 版本号</code></p>
<p>ps: 可以使用 <code>git reflog</code> 查询版本号</p>
<h4 id="三、工作区与暂存区区别"><a href="#三、工作区与暂存区区别" class="headerlink" title="三、工作区与暂存区区别"></a><strong>三、工作区与暂存区区别</strong></h4><p>工作区：就是你在电脑上看到的目录，比如目录下testgit里的文件(.git隐藏目录版本库除外)。或者以后需要再新建的目录文件等等都属于工作区范畴。<br>版本库(Repository)：工作区有一个隐藏目录.git,这个不属于工作区，这是版本库。其中版本库里面存了很多东西，其中最重要的就是stage(暂存区)，还有Git为我们自动创建了第一个分支master,以及指向master的一个指针HEAD。 </p>
<h4 id="四、撤销修改和删除文件"><a href="#四、撤销修改和删除文件" class="headerlink" title="四、撤销修改和删除文件"></a>四、撤销修改和删除文件</h4><p> 一、撤销修改 </p>
<p>当某个文件修改后，但是还没有提交，想要撤销修改时，可以采用一下方法：</p>
<p>1.如果我知道要删掉那些内容的话，直接手动更改去掉那些需要的文件，然后add添加到暂存区，最后commit掉。 </p>
<p>2.直接恢复到上一个版本。使用 <code>git reset --hard HEAD^ </code></p>
<p>3.使用命令：<code>git checkout -- test.txt</code></p>
<p>二、删除文件</p>
<p>1.直接手动删除</p>
<p>2.<code>rm filename</code></p>
<p>如果向彻底从文件库删除，可以执行 commit 命令提交掉</p>
<p>在没有提交前，可以使用<code>git checkout -- filename</code> 从版本库中恢复此文件 </p>
<h4 id="五、远程仓库"><a href="#五、远程仓库" class="headerlink" title="五、远程仓库"></a><strong>五、远程仓库</strong></h4><p>1.创建ssh key</p>
<p>打开命令行，输入：</p>
<p><code> ssh-keygen -t rsa –C “youremail@example.com"</code></p>
<p>然后就会在用户主目录生成.ssh文件夹，包含公钥 <code>idrsa.pub</code> 和私钥 <code>idrsa</code></p>
<p>2.登录 github, 打开” settings”中的SSH Keys页面，然后点击“Add SSH Key”,填上任意title，在Key文本框里黏贴id_rsa.pub 文件的内容, 点击 Add Key。</p>
<p>3.添加远程库</p>
<p><code>git remote add origin repositoryname</code></p>
<p>4.把本地仓库分支master内容推送到原仓库</p>
<p><code>git push -u origin master</code></p>
<p>由于远程库是空的，我们第一次推送master分支时，加上了 –u参数，Git不但会把本地的master分支内容推送的远程新的master分支，还会把本地的master分支和远程的master分支关联起来，在以后的推送或者拉取时就可以简化命令。 </p>
<p>5.从远程克隆仓库</p>
<p><code>git clone repostoryname</code></p>
<h4 id="六、创建与合并分支"><a href="#六、创建与合并分支" class="headerlink" title="六、创建与合并分支"></a><strong>六、创建与合并分支</strong></h4><p>1.基本命令</p>
<p>查看分支：<code>git branch</code></p>
<p>创建分支：<code>it branch name</code></p>
<p>切换分支：<code>git checkout name</code></p>
<p>创建+切换分支：<code>git checkout –b name</code></p>
<p>合并某分支到当前分支：<code>git merge name</code></p>
<p>删除分支：<code>git branch –d name</code></p>
<p>2.如何解决冲突？ </p>
<p>在冲突时查看内容，修改成和主干代码一样的</p>
<p>3.分支管理策略 </p>
<p>通常合并分支时，git一般使用”Fast forward”模式，在这种模式下，删除分支后，会丢掉分支信息，可以使用带参数 –no-ff来禁用”Fast forward”模式，即<code>git merge --no-ff dev</code></p>
<p>分支策略：首先master主分支应该是非常稳定的，也就是用来发布新版本，一般情况下不允许在上面干活，干活一般情况下在新建的 dev 分支上干活，干完后，比如上要发布，或者说 dev 分支代码稳定后可以合并到主分支master上来。 </p>
<h4 id="七、bug分支"><a href="#七、bug分支" class="headerlink" title="七、bug分支"></a><strong>七、bug分支</strong></h4><p>1.将当前工作现场隐藏</p>
<p><code>git stash</code></p>
<p>2.在master分支上创建临时分支</p>
<p><code>git checkout -b issue-404</code></p>
<p>3.修改文件并提交</p>
<p>4.切换到master分支，完成合并，并删除issue-404分支</p>
<figure class="highlight crmsh"><table><tbody><tr><td class="code"><pre><code class="hljs crmsh">git checkout <span class="hljs-keyword">master</span><br><span class="hljs-title">git</span> merge --no-ff -m <span class="hljs-string">"merge bug fix 404"</span> issue-<span class="hljs-number">404</span><br>git branch -d issue-<span class="hljs-number">404</span><br></code></pre></td></tr></tbody></table></figure>

<p>5.回到dev分支，搜索工作现场</p>
<figure class="highlight ebnf"><table><tbody><tr><td class="code"><pre><code class="hljs ebnf"><span class="hljs-attribute">git checkout dev</span><br><span class="hljs-attribute">git stash list</span><br></code></pre></td></tr></tbody></table></figure>

<p>6.恢复工作现场</p>
<p>首先恢复：<code>git stash apply</code>，然后删除stash内容： <code>git stash drop</code></p>
<p>或者直接恢复并删除：<code>git stash pop</code> </p>
<h4 id="八、多人协作"><a href="#八、多人协作" class="headerlink" title="八、多人协作"></a><strong>八、多人协作</strong></h4><p>当你从远程库克隆时候，实际上Git自动把本地的master分支和远程的master分支对应起来了，并且远程库的默认名称是origin。 </p>
<p>查看原仓库信息：<code>git remote</code></p>
<p>查看远程库的详细信息：<code>git remote -v</code></p>
<p>一、推送分支</p>
<p>主分支：<code>git push origin master</code></p>
<p>其它分支：<code>git push origin dev</code></p>
<p>二、抓取分支</p>
<p>当其它小伙伴对某个分支 推送了提交 ，而在我的 而我在我的目录文件下也对同样的文件同个地方作了修改，也试图推送到远程库时，就会推送失败。</p>
<p> 因为我的小伙伴最新提交的和我试图推送的有冲突，解决的办法也很简单，先用git pull把最新的提交从origin/dev抓下来，然后在本地合并，解决冲突，再推送。 </p>
<p>1.指定本地dev分支与远程origin/dev分支的链接</p>
<p><code>git branch --set-upstream dev origin/dev</code></p>
<p>2.抓取</p>
<p><code>git pull</code></p>
<p>3.解决冲突</p>
<p>4.提交再push</p>
<p>因此：多人协作工作模式一般是这样的：</p>
<p>首先，可以试图用git push origin branch-name推送自己的修改.<br>如果推送失败，则因为远程分支比你的本地更新早，需要先用git pull试图合并。<br>如果合并有冲突，则需要解决冲突，并在本地提交。再用git push origin branch-name推送。</p>
<p>参考资料：</p>
<p> <a href="https://zhuanlan.zhihu.com/p/30044692">Git使用教程</a> </p>
]]></content>
      <tags>
        <tag>Git</tag>
      </tags>
  </entry>
  <entry>
    <title>Room的基本使用</title>
    <url>/2022/05/10/Room%E7%9A%84%E5%9F%BA%E6%9C%AC%E4%BD%BF%E7%94%A8/</url>
    <content><![CDATA[<p>Room-数据库持久化存储</p>
<p>简介： Room提供了一个访问SQLite的抽象层，以便在利用SQLite的全部功能的同时进行流畅的数据库访问。 </p>
<span id="more"></span>

<p>处理大量结构化数据的应用, 能从在本地持久化数据中极大受益. 最常见的用例是缓存有关联的数据碎片. 以这种方式, 在设备不能访问网络的时候, 用户依然能够浏览离线内容. 任何用户发起的改变, 都应该在设备重新在线之后同步到服务器.</p>
<p>因为Room为你充分消除了这些顾虑, 使用Room而非SQLite是高度推荐的.</p>
<h3 id="Room的基本使用"><a href="#Room的基本使用" class="headerlink" title="Room的基本使用"></a>Room的基本使用</h3><h4 id="Room主要构件"><a href="#Room主要构件" class="headerlink" title="Room主要构件"></a>Room主要构件</h4><p>Room有3个主要构件</p>
<ul>
<li><p>Database: 包含了数据库持有者, 并对于连接应用上持久化的相关数据, 作为一个主要的访问点, 来服务. 注解了@Database的类应该满足以下条件:</p>
<p>1.继承了RoomDatabase的抽象类;</p>
<p>2.包含实体列表, 而这些实体与该注解之下数据库关联;</p>
<p>3.包含一个抽象方法, 无参且返回一个注解了@Dao的类;</p>
</li>
</ul>
<p>在运行时, 你可以通过调用Room.databaseBuilder()或者Room.inMemoryDatabaseBuilder()方法请求Database实例.</p>
<ul>
<li>Entity: 一个Entity对应数据库中的一张表。Entity类是Sqlite表结构对Java类的映射，在Java中可以看作一个	Model类。</li>
<li>DAO: 数据访问对象（Data Access Objects），我们通过它来访问数据。</li>
</ul>
<h4 id="案例"><a href="#案例" class="headerlink" title="案例"></a>案例</h4><p>1.添加依赖</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-type">def</span> <span class="hljs-variable">room_version</span> <span class="hljs-operator">=</span> <span class="hljs-string">"2.4.2"</span><br><br>implementation <span class="hljs-string">"androidx.room:room-runtime:$room_version"</span><br>annotationProcessor <span class="hljs-string">"androidx.room:room-compiler:$room_version"</span><br><br><span class="hljs-comment">// optional - Test helpers</span><br>testImplementation <span class="hljs-string">"androidx.room:room-testing:$room_vesion"</span><br></code></pre></td></tr></tbody></table></figure>

<p>2.创建一个关于Note的表</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">//Entity注解将Note类和数据库中的表Note_table对应起来，tableName可指定别名，默认为类名</span><br><span class="hljs-meta">@Entity(tableName = "Note_table")</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Note</span> {<br>    <span class="hljs-meta">@PrimaryKey(autoGenerate = true)</span><span class="hljs-comment">//PrimaryKey时主键,autoGenerate自增长</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span> id;<br><br>    <span class="hljs-comment">//ColumnInfo用于指定该字段存储在表中的名字，默认为属性名</span><br>    <span class="hljs-meta">@ColumnInfo(name = "note_title")</span><br>    <span class="hljs-keyword">private</span> String title;<br>    <span class="hljs-meta">@ColumnInfo(name = "note_context")</span><br>    <span class="hljs-keyword">private</span> String context;<br>    <span class="hljs-meta">@ColumnInfo(name = "note_date")</span><br>    <span class="hljs-keyword">private</span> String date;<br><br>    <span class="hljs-comment">//Room默认使用这个构造方法</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">Note</span><span class="hljs-params">(String title, String context)</span> {<br>        <span class="hljs-built_in">this</span>.title = title;<br>        <span class="hljs-built_in">this</span>.context = context;<br>        <span class="hljs-type">Date</span> <span class="hljs-variable">d</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Date</span>();<br>        <span class="hljs-type">SimpleDateFormat</span> <span class="hljs-variable">sdf</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">SimpleDateFormat</span>(<span class="hljs-string">"yyyy-MM-dd HH:mm:ss"</span>);<br>        <span class="hljs-built_in">this</span>.date = sdf.format(d);<br>    }<br><br>    <span class="hljs-comment">//Ignore可以让Room忽略这个构造器</span><br>    <span class="hljs-meta">@Ignore</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">Note</span><span class="hljs-params">(String title)</span> {<br>        <span class="hljs-built_in">this</span>.title = title;<br>    }<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-type">int</span> <span class="hljs-title function_">getId</span><span class="hljs-params">()</span> {<br>        <span class="hljs-keyword">return</span> id;<br>    }<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">setId</span><span class="hljs-params">(<span class="hljs-type">int</span> id)</span> {<br>        <span class="hljs-built_in">this</span>.id = id;<br>    }<br><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">getTitle</span><span class="hljs-params">()</span> {<br>        <span class="hljs-keyword">return</span> title;<br>    }<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">setTitle</span><span class="hljs-params">(String title)</span> {<br>        <span class="hljs-built_in">this</span>.title = title;<br>    }<br><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">getContext</span><span class="hljs-params">()</span> {<br>        <span class="hljs-keyword">return</span> context;<br>    }<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">setContext</span><span class="hljs-params">(String context)</span> {<br>        <span class="hljs-built_in">this</span>.context = context;<br>    }<br><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">getDate</span><span class="hljs-params">()</span> {<br>        <span class="hljs-keyword">return</span> date;<br>    }<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">setDate</span><span class="hljs-params">(String date)</span> {<br>        <span class="hljs-built_in">this</span>.date = date;<br>    }<br>}<br></code></pre></td></tr></tbody></table></figure>



<p>3.创建Dao用来操作Note表中的数据</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@Dao</span><span class="hljs-comment">//Database Access Object </span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">NoteDao</span> {<br>    <span class="hljs-meta">@Insert</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">InsertNote</span><span class="hljs-params">(Note... notes)</span>;<br><br>    <span class="hljs-meta">@Update</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">UpdateNote</span><span class="hljs-params">(Note... notes)</span>;<br><br>    <span class="hljs-meta">@Delete</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">DeleteNote</span><span class="hljs-params">(Note... notes)</span>;<br><br>    <span class="hljs-meta">@Query("DELETE FROM NOTE")</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">DeleteAllNotes</span><span class="hljs-params">()</span>;<br><br>    <span class="hljs-comment">//查询Note表中所有的Note</span><br>    <span class="hljs-meta">@Query("SELECT * FROM NOTE ORDER BY ID DESC")</span><br>    <span class="hljs-keyword">public</span> List&lt;Note&gt; <span class="hljs-title function_">getAllNotes</span><span class="hljs-params">()</span>;<br><br>    <span class="hljs-comment">//根据关键字查询Note表中所有的Note</span><br>    <span class="hljs-meta">@Query("SELECT * FROM NOTE WHERE note_title LIKE '%' || :content || '%' order by id desc")</span><br>    <span class="hljs-keyword">public</span> List&lt;Note&gt; <span class="hljs-title function_">searchNotes</span><span class="hljs-params">(String content)</span>;<br>}<br></code></pre></td></tr></tbody></table></figure>



<p>4.创建数据库</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">/**</span><br><span class="hljs-comment"> * 注解Database告诉系统这是Rooom数据库对象</span><br><span class="hljs-comment"> * entities指定该数据库有哪些表，多张表就用逗号分隔</span><br><span class="hljs-comment"> * version指定数据库版本号,升级时需要用到</span><br><span class="hljs-comment"> * 数据库继承自Roomdatabase</span><br><span class="hljs-comment"> */</span><br><span class="hljs-meta">@Database(entities = {Note.class},version = 1,exportSchema = false)</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">NoteDatabase</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">RoomDatabase</span> {<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> NoteDatabase INSTANCE;<br>    <br>    <span class="hljs-comment">//结合单例模式完成创建数据库实例</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> NoteDatabase <span class="hljs-title function_">getDatabase</span><span class="hljs-params">(Context context)</span>{<br>        <span class="hljs-keyword">if</span> (INSTANCE == <span class="hljs-literal">null</span>){<br>            INSTANCE = 	Room.databaseBuilder(context.getApplicationContext(),NoteDatabase.class,<span class="hljs-string">"note_database"</span>)<br>                    .allowMainThreadQueries()<br>                    .build();<br>        }<br>        <span class="hljs-keyword">return</span> INSTANCE;<br>    }<br><br>    <span class="hljs-comment">//将创建的Dao对象已抽象的形式返回</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> NoteDao <span class="hljs-title function_">getNoteDao</span><span class="hljs-params">()</span>;<br>}<br></code></pre></td></tr></tbody></table></figure>

<p>备注: 在实例化AppDatabase对象的时候, 你应该使用单例模式, 因为每一个RoomDatabase实例都是非常耗时的, 而且你也应该很少访问多个实例.</p>
<p>5.使用</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Test</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">AppCompatActivity</span> {<br>    <span class="hljs-keyword">private</span> Button mButtonInsert;<br>    <span class="hljs-keyword">private</span> Button mButtonUpdate;<br>    <span class="hljs-keyword">private</span> Button mButtonDelete;<br>    <span class="hljs-keyword">private</span> Button mButtonClear;<br>    <span class="hljs-keyword">private</span> TextView mTextView;<br>    <span class="hljs-keyword">private</span> NoteDao mNoteDao;<br>    <span class="hljs-keyword">private</span> NoteDatabase mNoteDatabase;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">onCreate</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> Bundle savedInstanceState)</span> {<br>        <span class="hljs-built_in">super</span>.onCreate(savedInstanceState);<br>        mTextView = (TextView) findViewById(R.id.textview);<br>        mNoteDatabase = NoteDatabase.getDatabase(<span class="hljs-built_in">this</span>);<br>        mNoteDao = mNoteDatabase.getNoteDao();<br>        updateUI();<br><br>        mButtonInsert =(Button) findViewById(R.id.btn_insert);<br>        mButtonInsert.setOnClickListener(<span class="hljs-keyword">new</span> <span class="hljs-title class_">View</span>.OnClickListener() {<br>            <span class="hljs-meta">@Override</span><br>            <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">onClick</span><span class="hljs-params">(View view)</span> {<br>                <span class="hljs-type">Note</span> <span class="hljs-variable">note1</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Note</span>(<span class="hljs-string">"a"</span>,<span class="hljs-string">"a"</span>);<br>                <span class="hljs-type">Note</span> <span class="hljs-variable">note2</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Note</span>(<span class="hljs-string">"b"</span>,<span class="hljs-string">"b"</span>);<br>                mNoteDao.InsertNote(note1,note2);<br>                updateUI();<br>            }<br>        });<br><br>        mButtonUpdate = (Button) findViewById(R.id.btn_update);<br>        mButtonUpdate.setOnClickListener(<span class="hljs-keyword">new</span> <span class="hljs-title class_">View</span>.OnClickListener() {<br>            <span class="hljs-meta">@Override</span><br>            <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">onClick</span><span class="hljs-params">(View view)</span> {<br>                <span class="hljs-type">Note</span> <span class="hljs-variable">note</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Note</span>(<span class="hljs-string">"c"</span>,<span class="hljs-string">"c"</span>);<br>                note.setId(<span class="hljs-number">1</span>);<br>                mNoteDao.UpdateNote(note);<br>                updateUI();<br>            }<br>        });<br><br>        mButtonDelete = (Button) findViewById(R.id.btn_delete);<br>        mButtonDelete.setOnClickListener(<span class="hljs-keyword">new</span> <span class="hljs-title class_">View</span>.OnClickListener() {<br>            <span class="hljs-meta">@Override</span><br>            <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">onClick</span><span class="hljs-params">(View view)</span> {<br>                <span class="hljs-type">Note</span> <span class="hljs-variable">note</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Note</span>();<br>                note.setId(<span class="hljs-number">2</span>);<br>                mNoteDao.DeleteNote(note);<br>                updateUI();<br>            }<br>        });<br><br>        mButtonClear = (Button) findViewById(R.id.btn_clear);<br>        mButtonClear.setOnClickListener(<span class="hljs-keyword">new</span> <span class="hljs-title class_">View</span>.OnClickListener() {<br>            <span class="hljs-meta">@Override</span><br>            <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">onClick</span><span class="hljs-params">(View view)</span> {<br>                mNoteDao.DeleteAllNotes();<br>                updateUI();<br>            }<br>        });<br>    }<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">updateUI</span><span class="hljs-params">()</span>{<br>        List&lt;Note&gt; notes = mNoteDao.getAllNotes();<br>        <span class="hljs-type">String</span> <span class="hljs-variable">text</span> <span class="hljs-operator">=</span> <span class="hljs-string">""</span>;<br>        <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">0</span>;i&lt;notes.size();i++){<br>            <span class="hljs-type">Note</span> <span class="hljs-variable">note</span> <span class="hljs-operator">=</span> notes.get(i);<br>            text += <span class="hljs-string">"Title:"</span> + note.getTitle() + <span class="hljs-string">"Context"</span> + note.getContext() +<span class="hljs-string">"\n"</span>;<br>        }<br>        mTextView.setText(text);<br>    }<span class="hljs-comment">//更新视图</span><br>}<br></code></pre></td></tr></tbody></table></figure>

<h3 id="Room与LiveData、ViewModel的结合使用"><a href="#Room与LiveData、ViewModel的结合使用" class="headerlink" title="Room与LiveData、ViewModel的结合使用"></a>Room与LiveData、ViewModel的结合使用</h3><h4 id="直接使用Room的劣势"><a href="#直接使用Room的劣势" class="headerlink" title="直接使用Room的劣势"></a>直接使用Room的劣势</h4><p>Android在UI线程不能执行耗时操作，但是为了测试，我们强制允许数据库在UI线程操作，当数据库发生变化时，我们重新从数据库中查询所有的数据再刷新视图，虽然暂时看不出有什么影响，但是当处理较大量的数据时就会造成延时，影响用户的体验，也很不方便，能不能使数据操作在另一个线程进行呢？</p>
<h4 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h4><ul>
<li><p>在以往我们数据发生变化时都需要再去在工作线程中查询一遍数据然后把数据再给到UI线程去刷新，但是现在我们希望数据发生变化时，能自动接收到通知拿到最新数据，怎么搞？LiveData帮我们来搞定，这样当Room数据库中的数据发生变化时，通过LiveData组件通知View层，就可以实现数据的更新了。</p>
</li>
<li><p>LiveData通常与ViewModel一起使用，ViewModel是用于存放数据的，因此我们可以将数据库放在ViewModel中进行实例化，但数据库的实例化需要用到Context，而ViewModel中最好不要传入Context，因此直接使用ViewModel不妥，那我们使用它的子类AndroidViewModel，其构造方法中含有Application参数，其作为Context的子类刚好可以用来初始化数据库实例。</p>
</li>
<li><p>为了让数据操作不在UI线程进行，而是在另一个线程进行，我们可以使用AsyncTask，将数据操作放在工作线程中进行。</p>
</li>
</ul>
<h4 id="案例优化"><a href="#案例优化" class="headerlink" title="案例优化"></a>案例优化</h4><p>1.修改Note表的Dao文件，我们希望当数据发生改变时，能够收到一个实时的通知，这里我们使用LiveData将数据进行包装，如下：</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">//使用LiveData将结果List&lt;Note&gt;包装起来</span><br><span class="hljs-meta">@Query("SELECT * FROM NOTE ORDER BY ID DESC")</span><br><span class="hljs-keyword">public</span> LiveData&lt;List&lt;Note&gt;&gt; <span class="hljs-title function_">getAllNotesLive</span><span class="hljs-params">()</span>;<br>    <br><span class="hljs-comment">//使用LiveData将结果List&lt;Note&gt;包装起来</span><br><span class="hljs-meta">@Query("SELECT * FROM NOTE WHERE note_title LIKE '%' || :content || '%' order by id desc")</span><br><span class="hljs-keyword">public</span> LiveData&lt;List&lt;Note&gt;&gt; <span class="hljs-title function_">searchNotesLive</span><span class="hljs-params">(String content)</span>;<br></code></pre></td></tr></tbody></table></figure>

<p>2.创建NoteViewModel类，在构造器中初始化数据库，并且添加增删查改的AsyncTask类</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">NoteViewModel</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">AndroidViewModel</span> {<br>    <span class="hljs-keyword">private</span> NoteDao mNoteDao;<br>    <span class="hljs-keyword">private</span> LiveData&lt;List&lt;Note&gt;&gt; allNotesLive;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">NoteViewModel</span><span class="hljs-params">(<span class="hljs-meta">@NonNull</span> Application application)</span> {<br>        <span class="hljs-built_in">super</span>(application);<br>        <span class="hljs-type">NoteDatabase</span> <span class="hljs-variable">noteDatabase</span> <span class="hljs-operator">=</span> NoteDatabase.getDatabase(getApplication());<br>        mNoteDao = noteDatabase.getNoteDao();<br>        allNotesLive = mNoteDao.getAllNotesLive();<span class="hljs-comment">//初始化数据库实例</span><br>    }<br><br>    <span class="hljs-keyword">public</span> LiveData&lt;List&lt;Note&gt;&gt; <span class="hljs-title function_">getAllNotesLive</span><span class="hljs-params">()</span>{<br>        <span class="hljs-keyword">return</span> allNotesLive;<br>    }<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">InsertNote</span><span class="hljs-params">(Note... notes)</span>{<br>        <span class="hljs-keyword">new</span> <span class="hljs-title class_">InsertAsyncTask</span>(mNoteDao).execute(notes);<br>    }<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">UpdateNote</span><span class="hljs-params">(Note... notes)</span>{<br>        <span class="hljs-keyword">new</span> <span class="hljs-title class_">UpdateAsyncTask</span>(mNoteDao).execute(notes);<br>    }<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">DeleteNote</span><span class="hljs-params">(Note... notes)</span>{<br>        <span class="hljs-keyword">new</span> <span class="hljs-title class_">DeleteAsyncTask</span>(mNoteDao).execute(notes);<br>    }<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">ClearNote</span><span class="hljs-params">(Note... notes)</span>{<br>        <span class="hljs-keyword">new</span> <span class="hljs-title class_">ClearAsyncTask</span>(mNoteDao).execute();<br>    }<br><br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">InsertAsyncTask</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">AsyncTask</span>&lt;Note,Void,Void&gt;{<br>        <span class="hljs-keyword">private</span> NoteDao mNoteDao;<br>        <span class="hljs-keyword">public</span> <span class="hljs-title function_">InsertAsyncTask</span><span class="hljs-params">(NoteDao noteDao)</span> {<br>            <span class="hljs-built_in">this</span>.mNoteDao = noteDao;<br>        }<br>        <span class="hljs-meta">@Override</span><br>        <span class="hljs-keyword">protected</span> Void <span class="hljs-title function_">doInBackground</span><span class="hljs-params">(Note... notes)</span> {<br>            mNoteDao.InsertNote(notes);<br>            <span class="hljs-keyword">return</span> <span class="hljs-literal">null</span>;<br>        }<br>    }<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">UpdateAsyncTask</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">AsyncTask</span>&lt;Note,Void,Void&gt;{<br>        <span class="hljs-keyword">private</span> NoteDao mNoteDao;<br>        <span class="hljs-keyword">public</span> <span class="hljs-title function_">UpdateAsyncTask</span><span class="hljs-params">(NoteDao noteDao)</span> {<br>            <span class="hljs-built_in">this</span>.mNoteDao = noteDao;<br>        }<br>        <span class="hljs-meta">@Override</span><br>        <span class="hljs-keyword">protected</span> Void <span class="hljs-title function_">doInBackground</span><span class="hljs-params">(Note... notes)</span> {<br>            mNoteDao.UpdateNote(notes);<br>            <span class="hljs-keyword">return</span> <span class="hljs-literal">null</span>;<br>        }<br>    }<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">DeleteAsyncTask</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">AsyncTask</span>&lt;Note,Void,Void&gt;{<br>        <span class="hljs-keyword">private</span> NoteDao mNoteDao;<br>        <span class="hljs-keyword">public</span> <span class="hljs-title function_">DeleteAsyncTask</span><span class="hljs-params">(NoteDao noteDao)</span> {<br>            <span class="hljs-built_in">this</span>.mNoteDao = noteDao;<br>        }<br>        <span class="hljs-meta">@Override</span><br>        <span class="hljs-keyword">protected</span> Void <span class="hljs-title function_">doInBackground</span><span class="hljs-params">(Note... notes)</span> {<br>            mNoteDao.DeleteNote(notes);<br>            <span class="hljs-keyword">return</span> <span class="hljs-literal">null</span>;<br>        }<br>    }<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ClearAsyncTask</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">AsyncTask</span>&lt;Void,Void,Void&gt;{<br>        <span class="hljs-keyword">private</span> NoteDao mNoteDao;<br>        <span class="hljs-keyword">public</span> <span class="hljs-title function_">ClearAsyncTask</span><span class="hljs-params">(NoteDao noteDao)</span> {<br>            <span class="hljs-built_in">this</span>.mNoteDao = noteDao;<br>        }<br>        <span class="hljs-meta">@Override</span><br>        <span class="hljs-keyword">protected</span> Void <span class="hljs-title function_">doInBackground</span><span class="hljs-params">(Void... voids)</span> {<br>            mNoteDao.DeleteAllNotes();<br>            <span class="hljs-keyword">return</span> <span class="hljs-literal">null</span>;<br>        }<br>    }<br>}<br></code></pre></td></tr></tbody></table></figure>

<p>3.在Activity中实例化NoteViewModel，并监听LiveData中数据的变化</p>
<figure class="highlight java"><table><tbody><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Test</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">AppCompatActivity</span> {<br>    <span class="hljs-keyword">private</span> Button mButtonInsert;<br>    <span class="hljs-keyword">private</span> Button mButtonUpdate;<br>    <span class="hljs-keyword">private</span> Button mButtonDelete;<br>    <span class="hljs-keyword">private</span> Button mButtonClear;<br>    <span class="hljs-keyword">private</span> TextView mTextView;<br>    <span class="hljs-keyword">private</span> NoteDao mNoteDao;<br>    <span class="hljs-keyword">private</span> NoteViewModel mViewModel;<br>    <span class="hljs-keyword">private</span> LiveData&lt;List&lt;Note&gt;&gt; allNotesLive;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">onCreate</span><span class="hljs-params">(<span class="hljs-meta">@Nullable</span> Bundle savedInstanceState)</span> {<br>        <span class="hljs-built_in">super</span>.onCreate(savedInstanceState);<br>        mTextView = (TextView) findViewById(R.id.textview);<br>        <span class="hljs-type">NoteDatabase</span> <span class="hljs-variable">noteDatabase</span> <span class="hljs-operator">=</span> NoteDatabase.getDatabase(getApplication());<br>        mNoteDao = noteDatabase.getNoteDao();<br>        mViewModel = <span class="hljs-keyword">new</span> <span class="hljs-title class_">NoteViewModel</span>(getApplication());<br>        allNotesLive = mViewModel.getAllNotesLive();<br><br>        allNotesLive.observe(<span class="hljs-built_in">this</span>, <span class="hljs-keyword">new</span> <span class="hljs-title class_">Observer</span>&lt;List&lt;Note&gt;&gt;() {<br>            <span class="hljs-meta">@Override</span><br>            <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">onChanged</span><span class="hljs-params">(List&lt;Note&gt; notes)</span> {<br>                <span class="hljs-type">String</span> <span class="hljs-variable">text</span> <span class="hljs-operator">=</span> <span class="hljs-string">""</span>;<br>                <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">0</span>;i&lt;notes.size();i++){<br>                    <span class="hljs-type">Note</span> <span class="hljs-variable">note</span> <span class="hljs-operator">=</span> notes.get(i);<br>                    text += <span class="hljs-string">"Title:"</span> + note.getTitle() + <span class="hljs-string">"Context"</span> + note.getContext() +<span class="hljs-string">"\n"</span>;<br>                }<br>                mTextView.setText(text);<br>            }<br>        });<br><br>        mButtonInsert =(Button) findViewById(R.id.btn_insert);<br>        mButtonInsert.setOnClickListener(<span class="hljs-keyword">new</span> <span class="hljs-title class_">View</span>.OnClickListener() {<br>            <span class="hljs-meta">@Override</span><br>            <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">onClick</span><span class="hljs-params">(View view)</span> {<br>                <span class="hljs-type">Note</span> <span class="hljs-variable">note1</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Note</span>(<span class="hljs-string">"a"</span>,<span class="hljs-string">"a"</span>);<br>                <span class="hljs-type">Note</span> <span class="hljs-variable">note2</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Note</span>(<span class="hljs-string">"b"</span>,<span class="hljs-string">"b"</span>);<br>                mViewModel.InsertNote(note1,note2);             <br>            }<br>        });<br><br>        mButtonUpdate = (Button) findViewById(R.id.btn_update);<br>        mButtonUpdate.setOnClickListener(<span class="hljs-keyword">new</span> <span class="hljs-title class_">View</span>.OnClickListener() {<br>            <span class="hljs-meta">@Override</span><br>            <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">onClick</span><span class="hljs-params">(View view)</span> {<br>                <span class="hljs-type">Note</span> <span class="hljs-variable">note</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Note</span>(<span class="hljs-string">"c"</span>,<span class="hljs-string">"c"</span>);<br>                note.setId(<span class="hljs-number">1</span>);<br>                mViewModel.UpdateNote(note);<br>            }<br>        });<br><br>        mButtonDelete = (Button) findViewById(R.id.btn_delete);<br>        mButtonDelete.setOnClickListener(<span class="hljs-keyword">new</span> <span class="hljs-title class_">View</span>.OnClickListener() {<br>            <span class="hljs-meta">@Override</span><br>            <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">onClick</span><span class="hljs-params">(View view)</span> {<br>                <span class="hljs-type">Note</span> <span class="hljs-variable">note</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Note</span>();<br>                note.setId(<span class="hljs-number">2</span>);<br>                mViewModel.DeleteNote(note);<br>            }<br>        });<br><br>        mButtonClear = (Button) findViewById(R.id.btn_clear);<br>        mButtonClear.setOnClickListener(<span class="hljs-keyword">new</span> <span class="hljs-title class_">View</span>.OnClickListener() {<br>            <span class="hljs-meta">@Override</span><br>            <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">onClick</span><span class="hljs-params">(View view)</span> {<br>                mViewModel.ClearNote();<br>            }<br>        });<br>    }<br><br>}<br></code></pre></td></tr></tbody></table></figure>

<h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>Room具有Entity、Dao、Database三大构件，数据库不允许在UI线程操作，可以通过AsyncTask将数据操作放在工作线程进行，通过ViewModel将数据存储在单独的地方。也可以使用LiveData将数据包装起来，当数据库中的数据有变动时，<code>onChanged</code>方法会被自动调用，在此方法中刷新数据即可。总的来说，Room的使用可比直接用SQLite简单多了，而且方便升级，还能够配合LiveData、ViewModel、AsyncTask等使用，将数据操作放在工作线程进行。</p>
]]></content>
      <categories>
        <category>android</category>
      </categories>
      <tags>
        <tag>Room</tag>
      </tags>
  </entry>
</search>
