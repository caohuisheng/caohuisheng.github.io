<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>test</title>
    <url>/2022/05/07/test/</url>
    <content><![CDATA[<p>Room-数据库持久化存储</p>
<p>简介： Room提供了一个访问SQLite的抽象层，以便在利用SQLite的全部功能的同时进行流畅的数据库访问。 </p>
<p>处理大量结构化数据的应用, 能从在本地持久化数据中极大受益. 最常见的用例是缓存有关联的数据碎片. 以这种方式, 在设备不能访问网络的时候, 用户依然能够浏览离线内容. 任何用户发起的改变, 都应该在设备重新在线之后同步到服务器.</p>
<p>因为Room为你充分消除了这些顾虑, 使用Room而非SQLite是高度推荐的.</p>
<span id="more"></span>

<h3 id="Room的基本使用"><a href="#Room的基本使用" class="headerlink" title="Room的基本使用"></a>Room的基本使用</h3><h4 id="Room主要构件"><a href="#Room主要构件" class="headerlink" title="Room主要构件"></a>Room主要构件</h4><p>Room有3个主要构件</p>
<ul>
<li><p>Database: 包含了数据库持有者, 并对于连接应用上持久化的相关数据, 作为一个主要的访问点, 来服务. 注解了@Database的类应该满足以下条件:</p>
<p>1.继承了RoomDatabase的抽象类;</p>
<p>2.包含实体列表, 而这些实体与该注解之下数据库关联;</p>
<p>3.包含一个抽象方法, 无参且返回一个注解了@Dao的类;</p>
</li>
</ul>
<p>在运行时, 你可以通过调用Room.databaseBuilder()或者Room.inMemoryDatabaseBuilder()方法请求Database实例.</p>
<ul>
<li>Entity: 一个Entity对应数据库中的一张表。Entity类是Sqlite表结构对Java类的映射，在Java中可以看作一个	Model类。</li>
<li>DAO: 数据访问对象（Data Access Objects），我们通过它来访问数据。</li>
</ul>
<h4 id="案例"><a href="#案例" class="headerlink" title="案例"></a>案例</h4><p>1.添加依赖</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">def room_version = &quot;2.4.2&quot;</span><br><span class="line"></span><br><span class="line">implementation &quot;androidx.room:room-runtime:$room_version&quot;</span><br><span class="line">annotationProcessor &quot;androidx.room:room-compiler:$room_version&quot;</span><br><span class="line"></span><br><span class="line">// optional - Test helpers</span><br><span class="line">testImplementation &quot;androidx.room:room-testing:$room_vesion&quot;</span><br></pre></td></tr></table></figure>

<p>2.创建一个关于Note的表</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//Entity注解将Note类和数据库中的表Note_table对应起来，tableName可指定别名，默认为类名</span><br><span class="line">@Entity(tableName = &quot;Note_table&quot;)</span><br><span class="line">public class Note &#123;</span><br><span class="line">    @PrimaryKey(autoGenerate = true)//PrimaryKey时主键,autoGenerate自增长</span><br><span class="line">    private int id;</span><br><span class="line"></span><br><span class="line">    //ColumnInfo用于指定该字段存储在表中的名字，默认为属性名</span><br><span class="line">    @ColumnInfo(name = &quot;note_title&quot;)</span><br><span class="line">    private String title;</span><br><span class="line">    @ColumnInfo(name = &quot;note_context&quot;)</span><br><span class="line">    private String context;</span><br><span class="line">    @ColumnInfo(name = &quot;note_date&quot;)</span><br><span class="line">    private String date;</span><br><span class="line"></span><br><span class="line">    //Room默认使用这个构造方法</span><br><span class="line">    public Note(String title, String context) &#123;</span><br><span class="line">        this.title = title;</span><br><span class="line">        this.context = context;</span><br><span class="line">        Date d = new Date();</span><br><span class="line">        SimpleDateFormat sdf = new SimpleDateFormat(&quot;yyyy-MM-dd HH:mm:ss&quot;);</span><br><span class="line">        this.date = sdf.format(d);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    //Ignore可以让Room忽略这个构造器</span><br><span class="line">    @Ignore</span><br><span class="line">    public Note(String title) &#123;</span><br><span class="line">        this.title = title;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public int getId() &#123;</span><br><span class="line">        return id;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void setId(int id) &#123;</span><br><span class="line">        this.id = id;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public String getTitle() &#123;</span><br><span class="line">        return title;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void setTitle(String title) &#123;</span><br><span class="line">        this.title = title;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public String getContext() &#123;</span><br><span class="line">        return context;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void setContext(String context) &#123;</span><br><span class="line">        this.context = context;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public String getDate() &#123;</span><br><span class="line">        return date;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void setDate(String date) &#123;</span><br><span class="line">        this.date = date;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>3.创建Dao用来操作Note表中的数据</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">@Dao//Database Access Object </span><br><span class="line">public interface NoteDao &#123;</span><br><span class="line">    @Insert</span><br><span class="line">    public void InsertNote(Note... notes);</span><br><span class="line"></span><br><span class="line">    @Update</span><br><span class="line">    public void UpdateNote(Note... notes);</span><br><span class="line"></span><br><span class="line">    @Delete</span><br><span class="line">    public void DeleteNote(Note... notes);</span><br><span class="line"></span><br><span class="line">    @Query(&quot;DELETE FROM NOTE&quot;)</span><br><span class="line">    public void DeleteAllNotes();</span><br><span class="line"></span><br><span class="line">    //查询Note表中所有的Note</span><br><span class="line">    @Query(&quot;SELECT * FROM NOTE ORDER BY ID DESC&quot;)</span><br><span class="line">    public List&lt;Note&gt; getAllNotes();</span><br><span class="line"></span><br><span class="line">    //根据关键字查询Note表中所有的Note</span><br><span class="line">    @Query(&quot;SELECT * FROM NOTE WHERE note_title LIKE &#x27;%&#x27; || :content || &#x27;%&#x27; order by id desc&quot;)</span><br><span class="line">    public List&lt;Note&gt; searchNotes(String content);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>4.创建数据库</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/**</span><br><span class="line"> * 注解Database告诉系统这是Rooom数据库对象</span><br><span class="line"> * entities指定该数据库有哪些表，多张表就用逗号分隔</span><br><span class="line"> * version指定数据库版本号,升级时需要用到</span><br><span class="line"> * 数据库继承自Roomdatabase</span><br><span class="line"> */</span><br><span class="line">@Database(entities = &#123;Note.class&#125;,version = 1,exportSchema = false)</span><br><span class="line">public abstract class NoteDatabase extends RoomDatabase &#123;</span><br><span class="line">    private static NoteDatabase INSTANCE;</span><br><span class="line">    </span><br><span class="line">    //结合单例模式完成创建数据库实例</span><br><span class="line">    public static NoteDatabase getDatabase(Context context)&#123;</span><br><span class="line">        if (INSTANCE == null)&#123;</span><br><span class="line">            INSTANCE = 	Room.databaseBuilder(context.getApplicationContext(),NoteDatabase.class,&quot;note_database&quot;)</span><br><span class="line">                    .allowMainThreadQueries()</span><br><span class="line">                    .build();</span><br><span class="line">        &#125;</span><br><span class="line">        return INSTANCE;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    //将创建的Dao对象已抽象的形式返回</span><br><span class="line">    public abstract NoteDao getNoteDao();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>备注: 在实例化AppDatabase对象的时候, 你应该使用单例模式, 因为每一个RoomDatabase实例都是非常耗时的, 而且你也应该很少访问多个实例.</p>
<p>5.使用</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class Test extends AppCompatActivity &#123;</span><br><span class="line">    private Button mButtonInsert;</span><br><span class="line">    private Button mButtonUpdate;</span><br><span class="line">    private Button mButtonDelete;</span><br><span class="line">    private Button mButtonClear;</span><br><span class="line">    private TextView mTextView;</span><br><span class="line">    private NoteDao mNoteDao;</span><br><span class="line">    private NoteDatabase mNoteDatabase;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    protected void onCreate(@Nullable Bundle savedInstanceState) &#123;</span><br><span class="line">        super.onCreate(savedInstanceState);</span><br><span class="line">        mTextView = (TextView) findViewById(R.id.textview);</span><br><span class="line">        mNoteDatabase = NoteDatabase.getDatabase(this);</span><br><span class="line">        mNoteDao = mNoteDatabase.getNoteDao();</span><br><span class="line">        updateUI();</span><br><span class="line"></span><br><span class="line">        mButtonInsert =(Button) findViewById(R.id.btn_insert);</span><br><span class="line">        mButtonInsert.setOnClickListener(new View.OnClickListener() &#123;</span><br><span class="line">            @Override</span><br><span class="line">            public void onClick(View view) &#123;</span><br><span class="line">                Note note1 = new Note(&quot;a&quot;,&quot;a&quot;);</span><br><span class="line">                Note note2 = new Note(&quot;b&quot;,&quot;b&quot;);</span><br><span class="line">                mNoteDao.InsertNote(note1,note2);</span><br><span class="line">                updateUI();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line">        mButtonUpdate = (Button) findViewById(R.id.btn_update);</span><br><span class="line">        mButtonUpdate.setOnClickListener(new View.OnClickListener() &#123;</span><br><span class="line">            @Override</span><br><span class="line">            public void onClick(View view) &#123;</span><br><span class="line">                Note note = new Note(&quot;c&quot;,&quot;c&quot;);</span><br><span class="line">                note.setId(1);</span><br><span class="line">                mNoteDao.UpdateNote(note);</span><br><span class="line">                updateUI();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line">        mButtonDelete = (Button) findViewById(R.id.btn_delete);</span><br><span class="line">        mButtonDelete.setOnClickListener(new View.OnClickListener() &#123;</span><br><span class="line">            @Override</span><br><span class="line">            public void onClick(View view) &#123;</span><br><span class="line">                Note note = new Note();</span><br><span class="line">                note.setId(2);</span><br><span class="line">                mNoteDao.DeleteNote(note);</span><br><span class="line">                updateUI();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line">        mButtonClear = (Button) findViewById(R.id.btn_clear);</span><br><span class="line">        mButtonClear.setOnClickListener(new View.OnClickListener() &#123;</span><br><span class="line">            @Override</span><br><span class="line">            public void onClick(View view) &#123;</span><br><span class="line">                mNoteDao.DeleteAllNotes();</span><br><span class="line">                updateUI();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void updateUI()&#123;</span><br><span class="line">        List&lt;Note&gt; notes = mNoteDao.getAllNotes();</span><br><span class="line">        String text = &quot;&quot;;</span><br><span class="line">        for(int i=0;i&lt;notes.size();i++)&#123;</span><br><span class="line">            Note note = notes.get(i);</span><br><span class="line">            text += &quot;Title:&quot; + note.getTitle() + &quot;Context&quot; + note.getContext() +&quot;\n&quot;;</span><br><span class="line">        &#125;</span><br><span class="line">        mTextView.setText(text);</span><br><span class="line">    &#125;//更新视图</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="Room与LiveData、ViewModel的结合使用"><a href="#Room与LiveData、ViewModel的结合使用" class="headerlink" title="Room与LiveData、ViewModel的结合使用"></a>Room与LiveData、ViewModel的结合使用</h3><h4 id="直接使用Room的劣势"><a href="#直接使用Room的劣势" class="headerlink" title="直接使用Room的劣势"></a>直接使用Room的劣势</h4><p>Android在UI线程不能执行耗时操作，但是为了测试，我们强制允许数据库在UI线程操作，当数据库发生变化时，我们重新从数据库中查询所有的数据再刷新视图，虽然暂时看不出有什么影响，但是当处理较大量的数据时就会造成延时，影响用户的体验，也很不方便，能不能使数据操作在另一个线程进行呢？</p>
<h4 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h4><ul>
<li><p>在以往我们数据发生变化时都需要再去在工作线程中查询一遍数据然后把数据再给到UI线程去刷新，但是现在我们希望数据发生变化时，能自动接收到通知拿到最新数据，怎么搞？LiveData帮我们来搞定，这样当Room数据库中的数据发生变化时，通过LiveData组件通知View层，就可以实现数据的更新了。</p>
</li>
<li><p>LiveData通常与ViewModel一起使用，ViewModel是用于存放数据的，因此我们可以将数据库放在ViewModel中进行实例化，但数据库的实例化需要用到Context，而ViewModel中最好不要传入Context，因此直接使用ViewModel不妥，那我们使用它的子类AndroidViewModel，其构造方法中含有Application参数，其作为Context的子类刚好可以用来初始化数据库实例。</p>
</li>
<li><p>为了让数据操作不在UI线程进行，而是在另一个线程进行，我们可以使用AsyncTask，将数据操作放在工作线程中进行。</p>
</li>
</ul>
<h4 id="案例优化"><a href="#案例优化" class="headerlink" title="案例优化"></a>案例优化</h4><p>1.修改Note表的Dao文件，我们希望当数据发生改变时，能够收到一个实时的通知，这里我们使用LiveData将数据进行包装，如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//使用LiveData将结果List&lt;Note&gt;包装起来</span><br><span class="line">@Query(&quot;SELECT * FROM NOTE ORDER BY ID DESC&quot;)</span><br><span class="line">public LiveData&lt;List&lt;Note&gt;&gt; getAllNotesLive();</span><br><span class="line">    </span><br><span class="line">//使用LiveData将结果List&lt;Note&gt;包装起来</span><br><span class="line">@Query(&quot;SELECT * FROM NOTE WHERE note_title LIKE &#x27;%&#x27; || :content || &#x27;%&#x27; order by id desc&quot;)</span><br><span class="line">public LiveData&lt;List&lt;Note&gt;&gt; searchNotesLive(String content);</span><br></pre></td></tr></table></figure>

<p>2.创建NoteViewModel类，在构造器中初始化数据库，并且添加增删查改的AsyncTask类</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class NoteViewModel extends AndroidViewModel &#123;</span><br><span class="line">    private NoteDao mNoteDao;</span><br><span class="line">    private LiveData&lt;List&lt;Note&gt;&gt; allNotesLive;</span><br><span class="line"></span><br><span class="line">    public NoteViewModel(@NonNull Application application) &#123;</span><br><span class="line">        super(application);</span><br><span class="line">        NoteDatabase noteDatabase = NoteDatabase.getDatabase(getApplication());</span><br><span class="line">        mNoteDao = noteDatabase.getNoteDao();</span><br><span class="line">        allNotesLive = mNoteDao.getAllNotesLive();//初始化数据库实例</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public LiveData&lt;List&lt;Note&gt;&gt; getAllNotesLive()&#123;</span><br><span class="line">        return allNotesLive;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void InsertNote(Note... notes)&#123;</span><br><span class="line">        new InsertAsyncTask(mNoteDao).execute(notes);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void UpdateNote(Note... notes)&#123;</span><br><span class="line">        new UpdateAsyncTask(mNoteDao).execute(notes);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void DeleteNote(Note... notes)&#123;</span><br><span class="line">        new DeleteAsyncTask(mNoteDao).execute(notes);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void ClearNote(Note... notes)&#123;</span><br><span class="line">        new ClearAsyncTask(mNoteDao).execute();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    public static class InsertAsyncTask extends AsyncTask&lt;Note,Void,Void&gt;&#123;</span><br><span class="line">        private NoteDao mNoteDao;</span><br><span class="line">        public InsertAsyncTask(NoteDao noteDao) &#123;</span><br><span class="line">            this.mNoteDao = noteDao;</span><br><span class="line">        &#125;</span><br><span class="line">        @Override</span><br><span class="line">        protected Void doInBackground(Note... notes) &#123;</span><br><span class="line">            mNoteDao.InsertNote(notes);</span><br><span class="line">            return null;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public static class UpdateAsyncTask extends AsyncTask&lt;Note,Void,Void&gt;&#123;</span><br><span class="line">        private NoteDao mNoteDao;</span><br><span class="line">        public UpdateAsyncTask(NoteDao noteDao) &#123;</span><br><span class="line">            this.mNoteDao = noteDao;</span><br><span class="line">        &#125;</span><br><span class="line">        @Override</span><br><span class="line">        protected Void doInBackground(Note... notes) &#123;</span><br><span class="line">            mNoteDao.UpdateNote(notes);</span><br><span class="line">            return null;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public static class DeleteAsyncTask extends AsyncTask&lt;Note,Void,Void&gt;&#123;</span><br><span class="line">        private NoteDao mNoteDao;</span><br><span class="line">        public DeleteAsyncTask(NoteDao noteDao) &#123;</span><br><span class="line">            this.mNoteDao = noteDao;</span><br><span class="line">        &#125;</span><br><span class="line">        @Override</span><br><span class="line">        protected Void doInBackground(Note... notes) &#123;</span><br><span class="line">            mNoteDao.DeleteNote(notes);</span><br><span class="line">            return null;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public static class ClearAsyncTask extends AsyncTask&lt;Void,Void,Void&gt;&#123;</span><br><span class="line">        private NoteDao mNoteDao;</span><br><span class="line">        public ClearAsyncTask(NoteDao noteDao) &#123;</span><br><span class="line">            this.mNoteDao = noteDao;</span><br><span class="line">        &#125;</span><br><span class="line">        @Override</span><br><span class="line">        protected Void doInBackground(Void... voids) &#123;</span><br><span class="line">            mNoteDao.DeleteAllNotes();</span><br><span class="line">            return null;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>3.在Activity中实例化NoteViewModel，并监听LiveData中数据的变化</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class Test extends AppCompatActivity &#123;</span><br><span class="line">    private Button mButtonInsert;</span><br><span class="line">    private Button mButtonUpdate;</span><br><span class="line">    private Button mButtonDelete;</span><br><span class="line">    private Button mButtonClear;</span><br><span class="line">    private TextView mTextView;</span><br><span class="line">    private NoteDao mNoteDao;</span><br><span class="line">    private NoteViewModel mViewModel;</span><br><span class="line">    private LiveData&lt;List&lt;Note&gt;&gt; allNotesLive;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    protected void onCreate(@Nullable Bundle savedInstanceState) &#123;</span><br><span class="line">        super.onCreate(savedInstanceState);</span><br><span class="line">        mTextView = (TextView) findViewById(R.id.textview);</span><br><span class="line">        NoteDatabase noteDatabase = NoteDatabase.getDatabase(getApplication());</span><br><span class="line">        mNoteDao = noteDatabase.getNoteDao();</span><br><span class="line">        mViewModel = new NoteViewModel(getApplication());</span><br><span class="line">        allNotesLive = mViewModel.getAllNotesLive();</span><br><span class="line"></span><br><span class="line">        allNotesLive.observe(this, new Observer&lt;List&lt;Note&gt;&gt;() &#123;</span><br><span class="line">            @Override</span><br><span class="line">            public void onChanged(List&lt;Note&gt; notes) &#123;</span><br><span class="line">                String text = &quot;&quot;;</span><br><span class="line">                for(int i=0;i&lt;notes.size();i++)&#123;</span><br><span class="line">                    Note note = notes.get(i);</span><br><span class="line">                    text += &quot;Title:&quot; + note.getTitle() + &quot;Context&quot; + note.getContext() +&quot;\n&quot;;</span><br><span class="line">                &#125;</span><br><span class="line">                mTextView.setText(text);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line">        mButtonInsert =(Button) findViewById(R.id.btn_insert);</span><br><span class="line">        mButtonInsert.setOnClickListener(new View.OnClickListener() &#123;</span><br><span class="line">            @Override</span><br><span class="line">            public void onClick(View view) &#123;</span><br><span class="line">                Note note1 = new Note(&quot;a&quot;,&quot;a&quot;);</span><br><span class="line">                Note note2 = new Note(&quot;b&quot;,&quot;b&quot;);</span><br><span class="line">                mViewModel.InsertNote(note1,note2);             </span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line">        mButtonUpdate = (Button) findViewById(R.id.btn_update);</span><br><span class="line">        mButtonUpdate.setOnClickListener(new View.OnClickListener() &#123;</span><br><span class="line">            @Override</span><br><span class="line">            public void onClick(View view) &#123;</span><br><span class="line">                Note note = new Note(&quot;c&quot;,&quot;c&quot;);</span><br><span class="line">                note.setId(1);</span><br><span class="line">                mViewModel.UpdateNote(note);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line">        mButtonDelete = (Button) findViewById(R.id.btn_delete);</span><br><span class="line">        mButtonDelete.setOnClickListener(new View.OnClickListener() &#123;</span><br><span class="line">            @Override</span><br><span class="line">            public void onClick(View view) &#123;</span><br><span class="line">                Note note = new Note();</span><br><span class="line">                note.setId(2);</span><br><span class="line">                mViewModel.DeleteNote(note);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line">        mButtonClear = (Button) findViewById(R.id.btn_clear);</span><br><span class="line">        mButtonClear.setOnClickListener(new View.OnClickListener() &#123;</span><br><span class="line">            @Override</span><br><span class="line">            public void onClick(View view) &#123;</span><br><span class="line">                mViewModel.ClearNote();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>Room具有Entity、Dao、Database三大构件，数据库不允许在UI线程操作，可以通过AsyncTask将数据操作放在工作线程进行，通过ViewModel将数据存储在单独的地方。也可以使用LiveData将数据包装起来，当数据库中的数据有变动时，<code>onChanged</code>方法会被自动调用，在此方法中刷新数据即可。总的来说，Room的使用可比直接用SQLite简单多了，而且方便升级，还能够配合LiveData、ViewModel、AsyncTask等使用，将数据操作放在工作线程进行。</p>
]]></content>
  </entry>
  <entry>
    <title></title>
    <url>/2022/05/07/Room%E7%9A%84%E5%9F%BA%E6%9C%AC%E4%BD%BF%E7%94%A8/</url>
    <content><![CDATA[<p>Room-数据库持久化存储</p>
<p>简介： Room提供了一个访问SQLite的抽象层，以便在利用SQLite的全部功能的同时进行流畅的数据库访问。 </p>
<p>处理大量结构化数据的应用, 能从在本地持久化数据中极大受益. 最常见的用例是缓存有关联的数据碎片. 以这种方式, 在设备不能访问网络的时候, 用户依然能够浏览离线内容. 任何用户发起的改变, 都应该在设备重新在线之后同步到服务器.</p>
<p>因为Room为你充分消除了这些顾虑, 使用Room而非SQLite是高度推荐的.</p>
<span id="more"></span>

<h3 id="Room的基本使用"><a href="#Room的基本使用" class="headerlink" title="Room的基本使用"></a>Room的基本使用</h3><h4 id="Room主要构件"><a href="#Room主要构件" class="headerlink" title="Room主要构件"></a>Room主要构件</h4><p>Room有3个主要构件</p>
<ul>
<li><p>Database: 包含了数据库持有者, 并对于连接应用上持久化的相关数据, 作为一个主要的访问点, 来服务. 注解了@Database的类应该满足以下条件:</p>
<p>1.继承了RoomDatabase的抽象类;</p>
<p>2.包含实体列表, 而这些实体与该注解之下数据库关联;</p>
<p>3.包含一个抽象方法, 无参且返回一个注解了@Dao的类;</p>
</li>
</ul>
<p>在运行时, 你可以通过调用Room.databaseBuilder()或者Room.inMemoryDatabaseBuilder()方法请求Database实例.</p>
<ul>
<li>Entity: 一个Entity对应数据库中的一张表。Entity类是Sqlite表结构对Java类的映射，在Java中可以看作一个	Model类。</li>
<li>DAO: 数据访问对象（Data Access Objects），我们通过它来访问数据。</li>
</ul>
<h4 id="案例"><a href="#案例" class="headerlink" title="案例"></a>案例</h4><p>1.添加依赖</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">def room_version = &quot;2.4.2&quot;</span><br><span class="line"></span><br><span class="line">implementation &quot;androidx.room:room-runtime:$room_version&quot;</span><br><span class="line">annotationProcessor &quot;androidx.room:room-compiler:$room_version&quot;</span><br><span class="line"></span><br><span class="line">// optional - Test helpers</span><br><span class="line">testImplementation &quot;androidx.room:room-testing:$room_vesion&quot;</span><br></pre></td></tr></table></figure>

<p>2.创建一个关于Note的表</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//Entity注解将Note类和数据库中的表Note_table对应起来，tableName可指定别名，默认为类名</span><br><span class="line">@Entity(tableName = &quot;Note_table&quot;)</span><br><span class="line">public class Note &#123;</span><br><span class="line">    @PrimaryKey(autoGenerate = true)//PrimaryKey时主键,autoGenerate自增长</span><br><span class="line">    private int id;</span><br><span class="line"></span><br><span class="line">    //ColumnInfo用于指定该字段存储在表中的名字，默认为属性名</span><br><span class="line">    @ColumnInfo(name = &quot;note_title&quot;)</span><br><span class="line">    private String title;</span><br><span class="line">    @ColumnInfo(name = &quot;note_context&quot;)</span><br><span class="line">    private String context;</span><br><span class="line">    @ColumnInfo(name = &quot;note_date&quot;)</span><br><span class="line">    private String date;</span><br><span class="line"></span><br><span class="line">    //Room默认使用这个构造方法</span><br><span class="line">    public Note(String title, String context) &#123;</span><br><span class="line">        this.title = title;</span><br><span class="line">        this.context = context;</span><br><span class="line">        Date d = new Date();</span><br><span class="line">        SimpleDateFormat sdf = new SimpleDateFormat(&quot;yyyy-MM-dd HH:mm:ss&quot;);</span><br><span class="line">        this.date = sdf.format(d);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    //Ignore可以让Room忽略这个构造器</span><br><span class="line">    @Ignore</span><br><span class="line">    public Note(String title) &#123;</span><br><span class="line">        this.title = title;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public int getId() &#123;</span><br><span class="line">        return id;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void setId(int id) &#123;</span><br><span class="line">        this.id = id;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public String getTitle() &#123;</span><br><span class="line">        return title;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void setTitle(String title) &#123;</span><br><span class="line">        this.title = title;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public String getContext() &#123;</span><br><span class="line">        return context;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void setContext(String context) &#123;</span><br><span class="line">        this.context = context;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public String getDate() &#123;</span><br><span class="line">        return date;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void setDate(String date) &#123;</span><br><span class="line">        this.date = date;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>3.创建Dao用来操作Note表中的数据</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">@Dao//Database Access Object </span><br><span class="line">public interface NoteDao &#123;</span><br><span class="line">    @Insert</span><br><span class="line">    public void InsertNote(Note... notes);</span><br><span class="line"></span><br><span class="line">    @Update</span><br><span class="line">    public void UpdateNote(Note... notes);</span><br><span class="line"></span><br><span class="line">    @Delete</span><br><span class="line">    public void DeleteNote(Note... notes);</span><br><span class="line"></span><br><span class="line">    @Query(&quot;DELETE FROM NOTE&quot;)</span><br><span class="line">    public void DeleteAllNotes();</span><br><span class="line"></span><br><span class="line">    //查询Note表中所有的Note</span><br><span class="line">    @Query(&quot;SELECT * FROM NOTE ORDER BY ID DESC&quot;)</span><br><span class="line">    public List&lt;Note&gt; getAllNotes();</span><br><span class="line"></span><br><span class="line">    //根据关键字查询Note表中所有的Note</span><br><span class="line">    @Query(&quot;SELECT * FROM NOTE WHERE note_title LIKE &#x27;%&#x27; || :content || &#x27;%&#x27; order by id desc&quot;)</span><br><span class="line">    public List&lt;Note&gt; searchNotes(String content);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>4.创建数据库</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/**</span><br><span class="line"> * 注解Database告诉系统这是Rooom数据库对象</span><br><span class="line"> * entities指定该数据库有哪些表，多张表就用逗号分隔</span><br><span class="line"> * version指定数据库版本号,升级时需要用到</span><br><span class="line"> * 数据库继承自Roomdatabase</span><br><span class="line"> */</span><br><span class="line">@Database(entities = &#123;Note.class&#125;,version = 1,exportSchema = false)</span><br><span class="line">public abstract class NoteDatabase extends RoomDatabase &#123;</span><br><span class="line">    private static NoteDatabase INSTANCE;</span><br><span class="line">    </span><br><span class="line">    //结合单例模式完成创建数据库实例</span><br><span class="line">    public static NoteDatabase getDatabase(Context context)&#123;</span><br><span class="line">        if (INSTANCE == null)&#123;</span><br><span class="line">            INSTANCE = 	Room.databaseBuilder(context.getApplicationContext(),NoteDatabase.class,&quot;note_database&quot;)</span><br><span class="line">                    .allowMainThreadQueries()</span><br><span class="line">                    .build();</span><br><span class="line">        &#125;</span><br><span class="line">        return INSTANCE;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    //将创建的Dao对象已抽象的形式返回</span><br><span class="line">    public abstract NoteDao getNoteDao();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>备注: 在实例化AppDatabase对象的时候, 你应该使用单例模式, 因为每一个RoomDatabase实例都是非常耗时的, 而且你也应该很少访问多个实例.</p>
<p>5.使用</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class Test extends AppCompatActivity &#123;</span><br><span class="line">    private Button mButtonInsert;</span><br><span class="line">    private Button mButtonUpdate;</span><br><span class="line">    private Button mButtonDelete;</span><br><span class="line">    private Button mButtonClear;</span><br><span class="line">    private TextView mTextView;</span><br><span class="line">    private NoteDao mNoteDao;</span><br><span class="line">    private NoteDatabase mNoteDatabase;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    protected void onCreate(@Nullable Bundle savedInstanceState) &#123;</span><br><span class="line">        super.onCreate(savedInstanceState);</span><br><span class="line">        mTextView = (TextView) findViewById(R.id.textview);</span><br><span class="line">        mNoteDatabase = NoteDatabase.getDatabase(this);</span><br><span class="line">        mNoteDao = mNoteDatabase.getNoteDao();</span><br><span class="line">        updateUI();</span><br><span class="line"></span><br><span class="line">        mButtonInsert =(Button) findViewById(R.id.btn_insert);</span><br><span class="line">        mButtonInsert.setOnClickListener(new View.OnClickListener() &#123;</span><br><span class="line">            @Override</span><br><span class="line">            public void onClick(View view) &#123;</span><br><span class="line">                Note note1 = new Note(&quot;a&quot;,&quot;a&quot;);</span><br><span class="line">                Note note2 = new Note(&quot;b&quot;,&quot;b&quot;);</span><br><span class="line">                mNoteDao.InsertNote(note1,note2);</span><br><span class="line">                updateUI();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line">        mButtonUpdate = (Button) findViewById(R.id.btn_update);</span><br><span class="line">        mButtonUpdate.setOnClickListener(new View.OnClickListener() &#123;</span><br><span class="line">            @Override</span><br><span class="line">            public void onClick(View view) &#123;</span><br><span class="line">                Note note = new Note(&quot;c&quot;,&quot;c&quot;);</span><br><span class="line">                note.setId(1);</span><br><span class="line">                mNoteDao.UpdateNote(note);</span><br><span class="line">                updateUI();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line">        mButtonDelete = (Button) findViewById(R.id.btn_delete);</span><br><span class="line">        mButtonDelete.setOnClickListener(new View.OnClickListener() &#123;</span><br><span class="line">            @Override</span><br><span class="line">            public void onClick(View view) &#123;</span><br><span class="line">                Note note = new Note();</span><br><span class="line">                note.setId(2);</span><br><span class="line">                mNoteDao.DeleteNote(note);</span><br><span class="line">                updateUI();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line">        mButtonClear = (Button) findViewById(R.id.btn_clear);</span><br><span class="line">        mButtonClear.setOnClickListener(new View.OnClickListener() &#123;</span><br><span class="line">            @Override</span><br><span class="line">            public void onClick(View view) &#123;</span><br><span class="line">                mNoteDao.DeleteAllNotes();</span><br><span class="line">                updateUI();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void updateUI()&#123;</span><br><span class="line">        List&lt;Note&gt; notes = mNoteDao.getAllNotes();</span><br><span class="line">        String text = &quot;&quot;;</span><br><span class="line">        for(int i=0;i&lt;notes.size();i++)&#123;</span><br><span class="line">            Note note = notes.get(i);</span><br><span class="line">            text += &quot;Title:&quot; + note.getTitle() + &quot;Context&quot; + note.getContext() +&quot;\n&quot;;</span><br><span class="line">        &#125;</span><br><span class="line">        mTextView.setText(text);</span><br><span class="line">    &#125;//更新视图</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="Room与LiveData、ViewModel的结合使用"><a href="#Room与LiveData、ViewModel的结合使用" class="headerlink" title="Room与LiveData、ViewModel的结合使用"></a>Room与LiveData、ViewModel的结合使用</h3><h4 id="直接使用Room的劣势"><a href="#直接使用Room的劣势" class="headerlink" title="直接使用Room的劣势"></a>直接使用Room的劣势</h4><p>Android在UI线程不能执行耗时操作，但是为了测试，我们强制允许数据库在UI线程操作，当数据库发生变化时，我们重新从数据库中查询所有的数据再刷新视图，虽然暂时看不出有什么影响，但是当处理较大量的数据时就会造成延时，影响用户的体验，也很不方便，能不能使数据操作在另一个线程进行呢？</p>
<h4 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h4><ul>
<li><p>在以往我们数据发生变化时都需要再去在工作线程中查询一遍数据然后把数据再给到UI线程去刷新，但是现在我们希望数据发生变化时，能自动接收到通知拿到最新数据，怎么搞？LiveData帮我们来搞定，这样当Room数据库中的数据发生变化时，通过LiveData组件通知View层，就可以实现数据的更新了。</p>
</li>
<li><p>LiveData通常与ViewModel一起使用，ViewModel是用于存放数据的，因此我们可以将数据库放在ViewModel中进行实例化，但数据库的实例化需要用到Context，而ViewModel中最好不要传入Context，因此直接使用ViewModel不妥，那我们使用它的子类AndroidViewModel，其构造方法中含有Application参数，其作为Context的子类刚好可以用来初始化数据库实例。</p>
</li>
<li><p>为了让数据操作不在UI线程进行，而是在另一个线程进行，我们可以使用AsyncTask，将数据操作放在工作线程中进行。</p>
</li>
</ul>
<h4 id="案例优化"><a href="#案例优化" class="headerlink" title="案例优化"></a>案例优化</h4><p>1.修改Note表的Dao文件，我们希望当数据发生改变时，能够收到一个实时的通知，这里我们使用LiveData将数据进行包装，如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//使用LiveData将结果List&lt;Note&gt;包装起来</span><br><span class="line">@Query(&quot;SELECT * FROM NOTE ORDER BY ID DESC&quot;)</span><br><span class="line">public LiveData&lt;List&lt;Note&gt;&gt; getAllNotesLive();</span><br><span class="line">    </span><br><span class="line">//使用LiveData将结果List&lt;Note&gt;包装起来</span><br><span class="line">@Query(&quot;SELECT * FROM NOTE WHERE note_title LIKE &#x27;%&#x27; || :content || &#x27;%&#x27; order by id desc&quot;)</span><br><span class="line">public LiveData&lt;List&lt;Note&gt;&gt; searchNotesLive(String content);</span><br></pre></td></tr></table></figure>

<p>2.创建NoteViewModel类，在构造器中初始化数据库，并且添加增删查改的AsyncTask类</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class NoteViewModel extends AndroidViewModel &#123;</span><br><span class="line">    private NoteDao mNoteDao;</span><br><span class="line">    private LiveData&lt;List&lt;Note&gt;&gt; allNotesLive;</span><br><span class="line"></span><br><span class="line">    public NoteViewModel(@NonNull Application application) &#123;</span><br><span class="line">        super(application);</span><br><span class="line">        NoteDatabase noteDatabase = NoteDatabase.getDatabase(getApplication());</span><br><span class="line">        mNoteDao = noteDatabase.getNoteDao();</span><br><span class="line">        allNotesLive = mNoteDao.getAllNotesLive();//初始化数据库实例</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public LiveData&lt;List&lt;Note&gt;&gt; getAllNotesLive()&#123;</span><br><span class="line">        return allNotesLive;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void InsertNote(Note... notes)&#123;</span><br><span class="line">        new InsertAsyncTask(mNoteDao).execute(notes);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void UpdateNote(Note... notes)&#123;</span><br><span class="line">        new UpdateAsyncTask(mNoteDao).execute(notes);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void DeleteNote(Note... notes)&#123;</span><br><span class="line">        new DeleteAsyncTask(mNoteDao).execute(notes);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void ClearNote(Note... notes)&#123;</span><br><span class="line">        new ClearAsyncTask(mNoteDao).execute();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    public static class InsertAsyncTask extends AsyncTask&lt;Note,Void,Void&gt;&#123;</span><br><span class="line">        private NoteDao mNoteDao;</span><br><span class="line">        public InsertAsyncTask(NoteDao noteDao) &#123;</span><br><span class="line">            this.mNoteDao = noteDao;</span><br><span class="line">        &#125;</span><br><span class="line">        @Override</span><br><span class="line">        protected Void doInBackground(Note... notes) &#123;</span><br><span class="line">            mNoteDao.InsertNote(notes);</span><br><span class="line">            return null;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public static class UpdateAsyncTask extends AsyncTask&lt;Note,Void,Void&gt;&#123;</span><br><span class="line">        private NoteDao mNoteDao;</span><br><span class="line">        public UpdateAsyncTask(NoteDao noteDao) &#123;</span><br><span class="line">            this.mNoteDao = noteDao;</span><br><span class="line">        &#125;</span><br><span class="line">        @Override</span><br><span class="line">        protected Void doInBackground(Note... notes) &#123;</span><br><span class="line">            mNoteDao.UpdateNote(notes);</span><br><span class="line">            return null;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public static class DeleteAsyncTask extends AsyncTask&lt;Note,Void,Void&gt;&#123;</span><br><span class="line">        private NoteDao mNoteDao;</span><br><span class="line">        public DeleteAsyncTask(NoteDao noteDao) &#123;</span><br><span class="line">            this.mNoteDao = noteDao;</span><br><span class="line">        &#125;</span><br><span class="line">        @Override</span><br><span class="line">        protected Void doInBackground(Note... notes) &#123;</span><br><span class="line">            mNoteDao.DeleteNote(notes);</span><br><span class="line">            return null;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public static class ClearAsyncTask extends AsyncTask&lt;Void,Void,Void&gt;&#123;</span><br><span class="line">        private NoteDao mNoteDao;</span><br><span class="line">        public ClearAsyncTask(NoteDao noteDao) &#123;</span><br><span class="line">            this.mNoteDao = noteDao;</span><br><span class="line">        &#125;</span><br><span class="line">        @Override</span><br><span class="line">        protected Void doInBackground(Void... voids) &#123;</span><br><span class="line">            mNoteDao.DeleteAllNotes();</span><br><span class="line">            return null;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>3.在Activity中实例化NoteViewModel，并监听LiveData中数据的变化</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class Test extends AppCompatActivity &#123;</span><br><span class="line">    private Button mButtonInsert;</span><br><span class="line">    private Button mButtonUpdate;</span><br><span class="line">    private Button mButtonDelete;</span><br><span class="line">    private Button mButtonClear;</span><br><span class="line">    private TextView mTextView;</span><br><span class="line">    private NoteDao mNoteDao;</span><br><span class="line">    private NoteViewModel mViewModel;</span><br><span class="line">    private LiveData&lt;List&lt;Note&gt;&gt; allNotesLive;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    protected void onCreate(@Nullable Bundle savedInstanceState) &#123;</span><br><span class="line">        super.onCreate(savedInstanceState);</span><br><span class="line">        mTextView = (TextView) findViewById(R.id.textview);</span><br><span class="line">        NoteDatabase noteDatabase = NoteDatabase.getDatabase(getApplication());</span><br><span class="line">        mNoteDao = noteDatabase.getNoteDao();</span><br><span class="line">        mViewModel = new NoteViewModel(getApplication());</span><br><span class="line">        allNotesLive = mViewModel.getAllNotesLive();</span><br><span class="line"></span><br><span class="line">        allNotesLive.observe(this, new Observer&lt;List&lt;Note&gt;&gt;() &#123;</span><br><span class="line">            @Override</span><br><span class="line">            public void onChanged(List&lt;Note&gt; notes) &#123;</span><br><span class="line">                String text = &quot;&quot;;</span><br><span class="line">                for(int i=0;i&lt;notes.size();i++)&#123;</span><br><span class="line">                    Note note = notes.get(i);</span><br><span class="line">                    text += &quot;Title:&quot; + note.getTitle() + &quot;Context&quot; + note.getContext() +&quot;\n&quot;;</span><br><span class="line">                &#125;</span><br><span class="line">                mTextView.setText(text);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line">        mButtonInsert =(Button) findViewById(R.id.btn_insert);</span><br><span class="line">        mButtonInsert.setOnClickListener(new View.OnClickListener() &#123;</span><br><span class="line">            @Override</span><br><span class="line">            public void onClick(View view) &#123;</span><br><span class="line">                Note note1 = new Note(&quot;a&quot;,&quot;a&quot;);</span><br><span class="line">                Note note2 = new Note(&quot;b&quot;,&quot;b&quot;);</span><br><span class="line">                mViewModel.InsertNote(note1,note2);             </span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line">        mButtonUpdate = (Button) findViewById(R.id.btn_update);</span><br><span class="line">        mButtonUpdate.setOnClickListener(new View.OnClickListener() &#123;</span><br><span class="line">            @Override</span><br><span class="line">            public void onClick(View view) &#123;</span><br><span class="line">                Note note = new Note(&quot;c&quot;,&quot;c&quot;);</span><br><span class="line">                note.setId(1);</span><br><span class="line">                mViewModel.UpdateNote(note);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line">        mButtonDelete = (Button) findViewById(R.id.btn_delete);</span><br><span class="line">        mButtonDelete.setOnClickListener(new View.OnClickListener() &#123;</span><br><span class="line">            @Override</span><br><span class="line">            public void onClick(View view) &#123;</span><br><span class="line">                Note note = new Note();</span><br><span class="line">                note.setId(2);</span><br><span class="line">                mViewModel.DeleteNote(note);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line">        mButtonClear = (Button) findViewById(R.id.btn_clear);</span><br><span class="line">        mButtonClear.setOnClickListener(new View.OnClickListener() &#123;</span><br><span class="line">            @Override</span><br><span class="line">            public void onClick(View view) &#123;</span><br><span class="line">                mViewModel.ClearNote();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>Room具有Entity、Dao、Database三大构件，数据库不允许在UI线程操作，可以通过AsyncTask将数据操作放在工作线程进行，通过ViewModel将数据存储在单独的地方。也可以使用LiveData将数据包装起来，当数据库中的数据有变动时，<code>onChanged</code>方法会被自动调用，在此方法中刷新数据即可。总的来说，Room的使用可比直接用SQLite简单多了，而且方便升级，还能够配合LiveData、ViewModel、AsyncTask等使用，将数据操作放在工作线程进行。</p>
]]></content>
  </entry>
  <entry>
    <title>Hello World</title>
    <url>/2022/05/06/hello-world/</url>
    <content><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p>
<h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p>
<h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p>
<h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p>
<h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>
]]></content>
  </entry>
</search>
